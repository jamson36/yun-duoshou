# 让你花个爽！活动网页产品需求文档（PRD）

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 让你花个爽！ |
| 产品形态 | 移动端优先的响应式网页 |
| 版本 | v1.2 消费人格与 DeepSeek 接入版 |
| 状态 | 全景、本地评分与 DeepSeek 后端已实现；真实密钥配置和阿里云部署待验收 |
| 更新日期 | 2026-08-25 |
| 主要用途 | 小型活动展示、现场体验与作品评审 |

本文档文字是活动版需求的唯一验收依据。流程图仅用于辅助理解；当图片与文字冲突时，以文字为准。

---

## 1. 项目介绍

### 1.1 产品背景

让你花个爽！面向外卖、奶茶和购物冲动频繁的年轻用户。

用户想买东西时，可以先在网页里完成一次“虚拟下单”。系统记录这次消费欲望，帮助用户完成冷静判断，并把最终没有购买的金额计入“确认省下”。

产品不连接银行卡，不发生真实支付或转账，也不承诺用户已经获得真实存款。

### 1.2 产品定位

让你花个爽！不是传统记账工具，而是一个以可环视的全景房间为主入口、以消费测试与 AI 钱包画像为核心的“消费冲动缓冲器”。房间是用户的钱包状态空间，其中的健身区屏幕、沙发手机、白板和快递堆同时承担导航、探索和状态反馈。

它帮助用户完成四个动作：

1. 先记录想买什么，延迟立即付款。
2. 决定“幸好没买”或“最终购买”。
3. 通过钱包病历本看见自己的消费习惯、冲动来源和消费人格。
4. 获得基于自身记录的 AI 行为建议，并把确认省下的金额映射到一个兴趣目标。

### 1.3 产品名称与传播语

- 正式名称：让你花个爽！
- 主 Slogan：假装下单，真的回血
- 补充解释：记录消费冲动，确认没有购买后，才计入省下金额

页面、海报、宣传视频和演示讲解统一使用“让你花个爽！”，不再混用其他候选名称。

### 1.4 核心功能

1. **空间化首页**：使用轻量 WebGL 球形全景承载首页数据、功能导航和状态反馈；产品表单仍使用语义化 DOM 内容层。
2. **虚拟下单与订单**：记录商品、金额、分类和购买原因，并处理订单结果。
3. **AI 钱包病历本**：结合 7 天 / 30 天订单、消费习惯和当前目标，输出可追溯的消费人格、模式解读和行动建议。
4. **回血计划**：把确认省下的金额映射为目标进度。

### 1.5 活动版交付边界

活动版交付一个可通过 HTTPS 链接访问的响应式网页，移动端优先，同时兼容桌面浏览器。

活动版具有以下边界：

- 不要求登录、注册或绑定手机号。
- 不接入真实支付、银行账户或电商订单。
- 接入 AI 钱包分析能力，但不接入商品抓取和消息推送服务。
- AI 只提供消费行为观察和可执行的日常建议，不提供投资、理财、信贷、医疗或心理诊断。
- 数据仅保存在当前浏览器，不跨设备同步。
- 只有用户主动发起 AI 分析并确认数据用途后，系统才会发送本次分析所必需的结构化汇总数据。
- 用户可以把个人数据导出为本地文件，自行保存备份。
- 提供演示数据和重置入口，保证现场可以即时展示周/月分析。
- 主流程应能在 90 秒内完成一次包含 AI 诊断的完整演示。

---

## 2. 核心定义

为避免把虚拟记录误写成真实存款，页面统一使用以下术语。

| 术语 | 定义 | 计入目标进度 |
| --- | --- | --- |
| 已记录金额 | 用户提交的全部未删除订单金额 | 否 |
| 冷静中 | 已提交，但用户还没有做最终决定 | 否 |
| 确认省下 | 用户明确选择“幸好没买”的订单金额 | 是 |
| 最终购买 | 用户自报最终仍然购买的订单金额 | 否 |
| 回血进度 | 目标建立后产生的“确认省下”金额之和 | 是 |
| AI 钱包诊断 | 基于系统已计算统计数据生成的消费行为解读和建议，不是财务或医疗诊断 | 否 |

“确认省下”是产品内的自我记录，不代表钱已经进入银行账户。

不得把冷静中的订单写成“实际省下”“存款到账”或“已经暴富”。

---

## 3. 产品目标

### 3.1 用户目标

帮助用户在消费冲动出现时完成：

1. **延迟冲动**：先记录，不立即购买。
2. **看见欲望**：知道自己主要想把钱花在哪里。
3. **确认结果**：区分最终购买和确认省下。
4. **理解模式**：通过数据与 AI 解读，知道哪些分类、情绪和诱因最容易触发冲动。
5. **获得行动建议**：获得有数据依据、不羞辱用户、可以立即实践的小建议。
6. **转换目标**：把确认省下映射为兴趣目标的虚拟进度。

### 3.2 活动展示目标

- 评审者无需登录即可开始体验。
- 首次进入时，能在 10 秒内理解“房间就是功能入口”，不需要学习游戏操作。
- 首次进入能快速理解“先假买，再决定”的核心玩法。
- 60 至 90 秒内可以完成下单、确认结果、查看 AI 诊断和目标进度。
- 使用演示数据时，可以立即展示最近 7 天和 30 天分析。
- 页面刷新后，当前浏览器中的数据仍然存在。

### 3.3 本版不承担的目标

- 不验证用户是否真的没有购买。
- 不提供理财建议、投资建议或真实储蓄服务。
- 不让 AI 自动修改订单状态、删除数据、更换目标或执行任何真实资金操作。
- 不以连续虚拟下单次数鼓励用户制造更多消费欲望。
- 不在活动版验证长期留存、真实财务改善或商业化能力。

---

## 4. 目标用户

| 用户类型 | 典型状态 | 核心痛点 | 典型行为 |
| --- | --- | --- | --- |
| 饭瘾爆发型年轻人 | 奶茶、外卖、夜宵和咖啡消费频繁 | 不知道小额消费累计去了哪里 | 下午想点奶茶，晚上想点夜宵，容易被满减吸引 |
| 冲动购物型年轻人 | 经常被种草、直播和限时优惠刺激 | 下单后后悔，但下次仍会重复 | 深夜刷商品、低价多买、购物车长期堆积 |
| 有兴趣目标但缺少预算的人 | 想旅行、健身、摄影或学习 | 目标预算被零散消费逐步挤占 | 想存钱，但缺少即时反馈和可视化进度 |

活动版优先服务 18 至 35 岁、熟悉移动网页和 Meme 表达的用户。

---

## 5. 核心流程与订单状态

### 5.1 主流程

1. 用户进入可左右环视的全景房间首页，通过房间数字和物件状态看到今日钱包概况。
2. 用户点击常驻的“我想买，先假买”，或点击沙发区茶几手机进入云下单。
3. 输入商品名称、金额，选择分类和购买原因。
4. 点击“确认云下单”，生成虚拟订单小票。
5. 订单进入“冷静中”，不计入确认省下。
6. 用户选择“幸好没买”或“我还是买了”。
7. 系统重新计算首页、钱包病历本和目标进度，房间中对应物件同步变化。
8. 用户进入钱包病历本，确认本次 AI 分析的数据范围后，获得消费人格、数据依据和行动建议。
9. 用户可以生成分享海报、调整回血目标或继续记录。

### 5.2 状态规则

```text
冷静中 → 幸好没买
冷静中 → 最终购买
幸好没买 ↔ 最终购买（允许用户纠正记录）
任意状态 → 删除（需二次确认）
```

状态变化后，所有统计结果必须立即重新计算。

活动版不依赖真实等待 24 小时。用户可以在提交后立即选择结果，也可以稍后从订单页处理。

### 5.3 核心场景

#### 场景一：想点奶茶

用户输入“珍珠奶茶”和 18 元，分类选择“餐饮饮品”，原因选择“嘴馋”，然后完成云下单。

订单先显示为“冷静中”。用户选择“幸好没买”后，确认省下增加 18 元；若已有目标且订单时间不早于目标创建时间，目标进度增加 18 元。

#### 场景二：想买衣服

用户输入“黑色工装裤”和 199 元，分类选择“服饰美妆”，原因选择“被种草”。

用户可以暂时保留为“冷静中”。活动版通过订单页提醒处理，不使用系统通知。

#### 场景三：现场查看分析

用户点击“加载演示数据”，系统生成最近 30 天的示例订单和一个示例目标。

钱包病历本立即展示高频分类、购买原因和基础人格。用户点击“请它看看”后，AI 使用明确标记的演示汇总数据生成解读和行动建议。演示数据和 AI 结果都必须带有明显的“演示”标识。

#### 场景四：询问 AI 钱包助手

用户点击房间中最醒目的病历柜或诊疗桌，镜头轻微推近，右侧展开钱包病历本。

系统先展示可验证的 7 天 / 30 天统计，再让用户确认是否发送本次分析所需的汇总数据。AI 结果至少包含一个消费人格、两条数据依据、一条行动建议和一条与当前目标的关联。

---

## 6. 页面与信息架构

活动版采用“空间化首页 + 功能内容层”的单页应用结构，包含四个一级页面、两个流程页面和一个内嵌在钱包病历本中的 AI 交互层。

### 6.1 一级页面

| 页面 | 核心内容 | 主要操作 |
| --- | --- | --- |
| 首页 | 全景房间、今日已记录、今日确认省下、冷静中数量、当前目标和快递内心 OS | 从场景热点进入三个功能区，或使用常驻文字导航 |
| 下单与订单 | 沙发区茶几手机对应的模拟下单与订单双页签界面 | 开始云下单、修改状态、编辑冷静中订单、删除订单、导出数据 |
| 消费测试 | 健身区竖屏对应的 7 天 / 30 天统计、高频分类、原因、AI 消费人格、依据和建议 | 切换周期、发起 AI 分析、选择引导式追问、生成分享海报 |
| 预算计划 | 白板对应的当前目标、目标金额、确认省下、进度和预计完成状态 | 创建、修改或替换目标 |

房间中的物件是桌面端的主导航，但不能成为唯一导航。移动端和“减少动态效果”模式必须保留清晰的底部导航；桌面端也需提供键盘可访问的文字导航。页面名称和顺序必须保持一致。

点击房间物件后，镜头在 300 至 500ms 内轻微推近，功能内容以侧边面板或全屏内容层展开。用户可以通过返回按钮、浏览器后退或 `Esc` 回到房间，且页面 URL 需与当前功能保持同步。

数据导出入口位于订单页的“数据管理”区域，不单独占用一级导航。

### 6.2 流程页面

| 页面 | 进入方式 | 完成条件 |
| --- | --- | --- |
| 云下单表单 | 首页主按钮 | 输入合法并成功创建订单 |
| 下单结果页 | 表单提交成功 | 用户选择结果或返回首页 |

### 6.3 首次进入与空状态

首次进入时，房间在不强制等待的前提下完成一次简短入场，并展示一句产品说明和两个入口：

- “我想买，先假买”：进入真实体验。
- “加载演示数据”：加载一组明确标记的示例数据。

首次进入需用一次性引导标出“健身区消费测试屏”、“沙发茶几手机”和“预算白板”三个主物件；用户完成引导后不再强制展示。

没有订单时，钱包病历本显示“还没有差点消费记录”，并引导用户开始云下单。

少于 3 笔订单时，不生成消费人格且不允许发起 AI 诊断，显示“再记录几次，才能看清钱包的性格”。

### 6.4 现有结构图

以下图片是 v1.0 阶段的辅助参考，尚未包含当前全景房间和 AI 钱包病历本，不作为 v1.1 的独立验收依据。当前“房间物件→一级页面”映射以本 PRD 和 `DESIGN.md` 为准；AI 阶段仍需补充“本地统计→数据确认→AI 诊断→降级”流程图。

![功能页面结构](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/a/M0BBex2OSaVQ9Gxe/3b7ada69f582476ba682555ef215856c0263.png)

![虚拟下单流程](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/a/M0BBex2OSaVQ9Gxe/f096285858c94ed88e41ac874e66dbdd0263.png)

![回血计划流程](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/a/M0BBex2OSaVQ9Gxe/acade0f06270458ea96085fd801bc1b20263.png)

![钱包病历本流程](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/a/M0BBex2OSaVQ9Gxe/6830d2351a214b9e85fd7f452f7b683e0263.png)

---

## 7. 功能范围

### 7.1 P0：活动版必须完成

| 模块 | 功能 | 规则与验收结果 |
| --- | --- | --- |
| 首次进入 | 强制像素瀑布开屏、主 IP 入口定格、开始体验、房间物件引导 | 无需登录；动画约 3 秒，按钮不提前出现且不可跳过；点击进入后同一全景镜头拉远 |
| 空间化首页 | 2:1 球形全景、今日状态、功能热点和常驻文字入口 | 正确显示已记录、确认省下和冷静中数量；可拖动环视，物件可点击、可聚焦且有文字名称 |
| 虚拟下单 | 商品、金额、分类、原因 | 校验通过后生成唯一订单 |
| 结果小票 | 展示本次记录 | 明确显示“尚未计入确认省下” |
| 订单列表 | 查看、筛选和处理订单 | 状态变化后统计立即更新 |
| AI 钱包病历本 | 7 天 / 30 天确定性统计与 AI 诊断 | 展示分类、原因、人格、数据依据、行动建议和目标关联 |
| AI 引导式追问 | 预置问题与当前会话 | 最多完成 3 轮引导式追问，不自动改写业务数据 |
| 回血计划 | 一个当前目标 | 仅确认省下金额增加目标进度 |
| 分享海报 | 生成预览和下载图片 | 不包含具体商品名和购买原因 |
| 本地数据 | 保存、演示、重置 | 刷新后保留；重置需二次确认 |
| 数据导出 | JSON 完整备份、CSV 订单表格 | 本地生成文件，默认排除演示数据 |
| AI 数据确认与降级 | 首次授权、数据摘要、超时 / 错误处理 | 用户明确确认后才请求 AI；不可用时回退为本地规则建议 |
| 响应式网页 | 手机、平板和桌面布局 | 375px、768px 与 1440px 宽度下房间可理解且主流程可完成 |

空间化首页的 P0 最低构成固定为：一个使用最终 2:1 场景图的球形全景、一个小浣熊主 IP、三个核心功能热点与一组快递探索热点，以及集成在健身区屏幕中的 AI 诊疗终端。首次进入先在同一全景的局部镜头中播放约 3 秒原创像素瀑布，动画结束才出现“小浣熊在左、让你花个爽！与进入按钮在右”的入口定格；点击后镜头平滑拉远到可交互房间。健身区屏幕对应消费测试，沙发区手机对应下单与订单，白板对应预算计划；点击后必须通过镜头转向、推近或等价空间转场聚焦物件，再进入该区域的专属交互。快递箱悬停、键盘聚焦或轻点时显示随机消费内心 OS。不得用普通卡片导航替代房间体验。小浣熊负责世界观与引导，AI 仍由诊疗终端承担。

#### 7.1.1 房间区域专属交互

| 区域 | 标志性动作 | 功能交互 | 完成反馈 |
| --- | --- | --- | --- |
| 沙发手机：假装下单 | 镜头靠近，手机从桌面轻抬并转正，屏幕亮起 | 使用手机式 DOM 表单记录商品、金额、分类和原因 | 生成虚拟小票并飞入订单收件箱，明确尚未计入确认省下 |
| 沙发手机：订单 | 手机切换订单页签，桌面展开小票堆 | 抬起单张小票查看并处理订单 | “幸好没买”盖绿色章并归入确认省下；“最终购买”盖橙红章并归档 |
| 健身区屏幕：钱包病历本 | 扫描光扫过，指标依次点亮，报告出现 | 查看本地统计、消费人格、数据依据与 AI 建议 | 诊断标签、AI 状态与“数据已变化”复诊提示清楚可辨 |
| 白板：预算计划 | 镜头将白板校正为近正视角 | 新增、填写、拖动、叠放和轻微旋转便签 | 保存时用胶带 / 图钉固定，目标进度线同步增长 |
| 快递堆：内心 OS | 箱体轻晃、胶带翘起，点击后开箱或前移 | 随机展示商品轮廓与一句消费内心 OS | 关闭后归位；分层素材齐备后可扩展后悔区 / 真香区分类 |

共同验收规则：同一时间只激活一个区域；镜头与物件进入动作以 300–500ms 为目标且不得阻塞操作；表单、订单详情和 AI 结果使用语义化 DOM；关闭、`Esc` 或浏览器后退时物件归位且焦点返回；“减少动态效果”模式用淡入、状态章和短距离位移替代浮起、飞入、扫描与旋转。

### 7.2 P1：活动后可增强

- 基于订单创建时间的 24 小时倒计时。
- 用户再次打开网页时提醒处理冷静中订单。
- 多目标管理和目标间分配金额。
- 欲望热力图、连续使用成就和更丰富的人格文案。
- 更自由的长对话、跨会话 AI 记忆，以及可选的用户自定义 AI 助手形象。
- 房间昼夜变化、更多数据驱动的物件成长和彩蛋。
- 在 P0 功能发现性与性能达标后，评估 Three.js 固定范围移动、靠近物件触发和更立体的镜头 / 灯光；不直接升级为无边界自由行走。
- 调用设备原生分享能力。
- 从备份文件导入数据。

### 7.3 P2：本次不实现

- 自动执行理财、投资、信贷或储蓄决策的 AI 顾问。
- AI 自动修改订单、目标或其他用户数据。
- 社区排行榜和用户社交。
- 真实商品搜索、电商跳转和订单同步。
- 登录、云端账户和多设备同步。
- 银行账户、支付和真实资金划转。
- 系统级定时推送。

---

## 8. 详细业务规则

### 8.1 虚拟下单字段

| 字段 | 规则 | 错误提示示例 |
| --- | --- | --- |
| 商品名称 | 必填，1 至 30 个字符 | 写下你刚刚想买什么 |
| 金额 | 必填，大于 0，不超过 99,999，最多两位小数 | 金额需要大于 0 |
| 分类 | 必选，使用固定分类 | 选一下这笔欲望属于哪里 |
| 购买原因 | 必选，使用固定原因 | 你是怎么被它打动的？ |
| 备注 | 选填，不超过 80 个字符 | 备注最多 80 个字符 |
| 创建时间 | 系统自动记录当前本地时间 | 不由用户填写 |

固定分类：餐饮饮品、服饰美妆、数码家居、娱乐社交、学习成长、旅行交通、其他。

固定原因：嘴馋、无聊、被种草、情绪不好、限时优惠、社交需要、自我提升、其他。

同名或同金额订单允许重复创建。连续点击提交时，按钮应进入处理中状态，避免重复订单。

冷静中的订单可以编辑；已经做出决定的订单仅允许修改状态或删除。

### 8.2 统计规则

- 今日数据按用户设备的本地日期计算。
- 7 天包含今天和之前 6 个自然日。
- 30 天包含今天和之前 29 个自然日。
- 已删除订单不参与任何统计。
- 金额展示统一保留两位小数；整数可以省略末尾 `.00`。
- 分类排行按金额降序；金额相同时按订单数量降序；仍相同时按最近一笔时间排序。
- 购买原因排行按订单次数降序。

首页至少展示：今日已记录金额、今日确认省下金额、冷静中订单数量。

钱包病历本至少展示：确认省下金额、最终购买金额、最高频分类、最高频原因、消费人格、人格依据、一条行动建议和与当前目标的关联。

金额、次数、排名、进度和统计周期必须由本地确定性逻辑计算。AI 只能解读这些结果，不能代替统计计算，也不能覆盖页面已展示的数字。

### 8.3 消费人格规则

消费人格使用用户选择的近 7 天或近 30 天数据；房间热点默认展示近 30 天结果。至少有 3 笔未删除的周期内订单后才生成初步人格。

消费人格不得再由最高频分类直接决定。系统先使用本地、版本化、可复算的规则生成五个行为维度：

| 维度 | 低分含义 | 高分含义 | 主要证据 |
| --- | --- | --- | --- |
| 即时冲动 I | 愿意延迟决定 | 触发后快速行动 | 触发原因、决策动作、决定时长 |
| 价格敏感 P | 较少关注价格规则 | 主动比价、找券、计算满减 | 限时优惠、比价、找券 / 凑满减 |
| 研究验证 R | 更多依赖直觉 | 主动查参数、评价和差评 | 查参数、比价、看评价、等待验证 |
| 外部依赖 X | 主要自己判断 | 明显依赖达人、朋友和评价 | 被种草、达人推荐、问朋友、社交需要 |
| 情绪体验 E | 功能和任务优先 | 疗愈、舒适、氛围和悦己优先 | 嘴馋、无聊、情绪不好、舒适 / 氛围 |

每个有证据的维度输出 0–100 分和维度置信度。没有直接或结构化证据的维度必须显示“证据不足”，不能使用 50 分伪装成真实中立。

系统同时计算四类购买动机占比：掌控、疗愈、成就和认同；并单独展示冷静单数率、冷静金额率、中位决定时间等行为结果。行为结果不参与道德评价。

本版人格库包括：研究主权者、优惠精算师、种草冲锋手、氛围疗愈师、社交共鸣者、微观成就家、极简缓冲者、收藏囤货派和平衡洞察者。每种人格都有直接证据门槛；没有任何人格达到阈值时显示“欲望观察员”。

页面显示一个主人格；第二名达到门槛且与第一名差值不超过 12 分时，可以同时显示一个副人格。3–4 笔记录只称为“初步倾向”；有效样本量和证据覆盖达到规则阈值后，才称为“相对稳定的近期画像”。

最高频分类只生成“钱包主题”，不再充当行为人格：

| 最高频分类 | 钱包主题 |
| --- | --- |
| 餐饮饮品 | 奶茶股东型选手 |
| 服饰美妆 | 种草收割机 |
| 数码家居 | 参数上头研究员 |
| 娱乐社交 | 快乐充值体验官 |
| 学习成长 | 自我提升囤积者 |
| 旅行交通 | 世界很大型选手 |
| 其他 | 欲望观察员 |

本地规则是分数、金额、次数、人格与置信度的唯一事实来源。DeepSeek 不能修改五维分数、人格名称或本地统计，只能结合本地结果进行复核解释、选择已有证据和生成一步建议。

人格用于趣味反馈和近期行为观察，不作为心理、财务或医疗诊断，不得使用“成瘾”“病态”“失控”等无证据诊断式标签。具体权重、公式、门槛和校准方法以 [`docs/plans/2026-08-25-consumption-personality-scoring-design.md`](./docs/plans/2026-08-25-consumption-personality-scoring-design.md) 为准。

### 8.4 建议生成规则

活动版同时保留“本地规则建议”和“AI 个性化建议”。本地规则是无网络、AI 超时、AI 配额用尽或用户不同意发送数据时的必备降级方案。

本地建议优先由最高频原因生成，并可结合本地主人格、冷静结果和当前目标调整表达，例如：

- 嘴馋：先喝一杯水，再给这笔欲望十分钟。
- 被种草：先保存商品，不保存付款冲动；明天同一时间再看一次。
- 限时优惠：限时的是优惠，不是你的决定时间。

当规则没有匹配时，使用通用建议：“先把这笔欲望放一会儿，再决定它是否值得。”

AI 建议必须满足：

- 至少引用一条当前周期内的统计事实，不得编造未记录的订单或目标。
- 默认只给出一个本周可执行的小动作，避免一次给出过多任务。
- 建议可以引导用户等待、减少触发、设定预算或关联当前目标，但不能承诺真实收益或替用户作出财务决策。
- 任何“更新目标”“标记订单”之类操作都只能作为建议呈现，必须由用户在对应功能页明确确认。

### 8.5 AI 数据与交互规则

AI 是钱包病历本的核心能力，但必须以本地统计为事实底座。

#### 发送前的数据确认

- 用户首次发起 AI 分析时，页面必须展示数据用途、发送范围和“不包含备注”说明，并获得明确同意。
- 默认发送：选定周期、分类次数与金额汇总、原因次数、订单状态汇总、确认省下总额、当前目标名称与进度，以及本地已计算的五维分数、动机占比、主人格、置信度和证据目录。
- 默认不发送：商品名称、订单备注、导出文件、浏览历史、真实身份、支付信息或银行信息。
- 消费人格和 AI 结果的生成时间需明确展示；订单变化后，旧结果标记为“数据已变化，建议重新分析”。

#### AI 输出结构

AI 首次诊断至少输出：

1. **消费人格解释**：使用本地已确定的人格名称，补充一句不羞辱的解释；不得更名或改分。
2. **数据依据**：从服务端提供的证据目录选择至少两条事实；AI 不得自由编写金额和次数。
3. **模式解读**：解释哪个分类或原因最值得注意。
4. **一步建议**：一个本周可执行的小动作。
5. **目标关联**：说明建议与当前回血目标的关系；没有目标时引导创建。
6. **边界说明**：结果基于用户自我记录，不是专业财务或医疗意见。

首次诊断后可提供最多 3 个引导式问题，例如“我这周最容易被什么触发？”“帮我定一个不会太狠的一周小计划”。活动版单次会话最多完成 3 轮追问，不提供无限对话。

#### 异常与降级

- AI 请求超时目标为 12 秒；超时、限流、内容校验失败或服务不可用时，展示明确错误和重试入口。
- 降级状态下仍显示本地统计、基础消费人格和固定规则建议，不得将规则结果伪装成 AI 回答。
- AI 返回的结构化结果必须经过字段校验和长度限制后再展示；校验不通过时进入降级流程。
- DeepSeek JSON 为空、截断或无法解析时，按无效结构处理，不能把半截文本直接展示给用户。

#### 本地保存与撤回

- 用户的 AI 数据发送同意状态只保存在当前浏览器，可以在“数据管理”中撤回。撤回后不删除订单和目标，下次发起 AI 分析时需重新确认。
- 活动版只在本地保存最近一次结构化 AI 诊断，新诊断覆盖旧诊断；引导式追问的完整对话不跨浏览器会话保存。
- 重置全部数据时，必须同时清除 AI 同意状态、最近一次诊断和当前会话。

### 8.6 回血计划规则

活动版只允许存在一个当前目标。

目标名称必填，1 至 20 个字符；目标金额必须大于 0；期望完成日期选填，但不得早于当天。

目标进度只累计目标创建时间之后的“确认省下”订单。冷静中和最终购买订单不得增加目标进度。

页面必须说明：“回血进度是虚拟记录，请根据实际情况自行存入真实账户。”

用户替换目标时，需要确认是否结束当前目标。修改目标名称或金额不会删除已有进度。

### 8.7 分享海报规则

海报采用 1080 × 1440 的竖版比例，默认展示：

- 让你花个爽！名称与 Slogan。
- 最近 7 天确认省下金额。
- 确认省下次数。
- 最高频分类和消费人格。
- 生成日期。
- “数据来自用户自我记录，不代表真实账户余额”的说明。

海报不得展示具体商品名、购买原因和备注。

网页提供图片预览和下载按钮。移动浏览器无法直接下载时，显示“长按图片保存”的提示。

### 8.8 本地数据规则

- 订单、目标、设置、AI 同意状态和最近一次结构化 AI 诊断仅保存在当前浏览器的本地存储中。AI 分析请求会按 8.5 节的明示范围暂时传输汇总数据，但不建立产品账户或云端历史诊断库。
- 页面需要说明“换设备、清除浏览器数据或使用无痕模式可能丢失记录”。
- 演示数据必须带有 `demo` 标记，并能一键清除。
- 加载演示数据前需提示是否覆盖当前数据。
- 重置全部数据前需二次确认。
- 数据结构应包含版本号，后续升级时可以迁移或安全重置。

### 8.9 数据导出规则

订单页提供“导出我的数据”入口。点击后打开导出面板，用户可以选择以下格式：

| 格式 | 用途 | 包含内容 |
| --- | --- | --- |
| JSON 完整备份 | 保存完整个人记录 | 数据版本、导出时间、货币、订单、当前目标和基础设置 |
| CSV 订单表格 | 使用表格软件查看 | 每一笔非演示订单及其主要字段 |

JSON 文件名使用 `让你花个爽！数据-YYYY-MM-DD.json`，结构至少包含：

```json
{
  "schemaVersion": 1,
  "exportedAt": "ISO 8601 时间",
  "currency": "CNY",
  "orders": [],
  "currentGoal": null,
  "settings": {}
}
```

CSV 文件名使用 `让你花个爽！订单-YYYY-MM-DD.csv`，字段顺序固定为：订单编号、商品名称、金额、分类、购买原因、备注、状态、创建时间、更新时间。

导出遵守以下规则：

- 文件在当前浏览器本地生成，不上传到服务器。
- 默认只导出个人数据，不包含带有 `demo` 标记的演示数据。
- 用户处于演示模式时，导出面板明确说明演示数据不会进入个人备份。
- 没有个人订单且没有目标时，导出按钮禁用并显示“暂无可导出的个人数据”。
- JSON 和 CSV 中的时间统一使用带时区的 ISO 8601 格式。
- CSV 使用 UTF-8 编码并兼容常见中文表格软件。
- CSV 必须正确处理逗号、换行和引号，并防止用户输入被表格软件作为公式执行。
- 导出前提示文件可能包含商品、原因和备注等私人记录，请用户妥善保存。
- 活动版只支持导出，不支持重新导入；数据导入保留到 P1。
- JSON 和 CSV 默认不导出 AI 同意状态、AI 诊断结果或 AI 会话内容。
- 移动浏览器无法直接下载时，应打开可保存的文件预览并给出操作提示。

---

## 9. 视觉与体验要求

### 9.1 参考网站与借鉴边界

- [Awwwards](https://www.awwwards.com/)：用于校准整体完成度、视觉创意、排版张力和动效细节，不作为单一页面模板。
- [basement.studio](https://basement.studio/)：重点参考“空间场景作为探索入口、可交互区域进入常规内容层、镜头进退提供反馈”的交互逻辑。
- [samsy.ninja](https://samsy.ninja/)：重点参考“直接走进数字空间、区域与装置承担功能、角色和环境持续反馈状态”的整体体验，用于启发钱包房间及未来钱包小城。
- [samsyninja-rebuild](https://github.com/boyang-hu/samsyninja-rebuild)：重点研究场景状态机、路由与场景同步、热点交互、事件桥，以及“镜像、逆向、溯源移植、里程碑验证、可读工程”的复刻流程；不复制其城市模型、贴图、角色、音视频、品牌或源码实现。
- [相关论坛讨论](https://linux.do/t/topic/2716188/22)：作为构建 `DESIGN.md` 和交互复刻流程的方法线索；具体内容只有在可访问并完成核对后才进入项目事实依据。
- [Lisa / Locomotive](https://lisa.locomotive.ca/en)：重点参考高对比黑白基底、鲜明紫色强调、大字号标题、规则网格、宽松留白和滚动叙事节奏。

参考目标是吸收设计语言，不复制品牌标识、文案、图片、视频、页面结构或受版权保护的素材。

让你花个爽！首先是可操作的产品网页。房间必须使功能更好理解，而不是变成隐藏入口的解谜游戏。P0 只保留一次约 3 秒、不可跳过的品牌开屏；更长的叙事开场、滚动劫持、复杂自定义光标和重型后处理不进入 P0。进入房间后，轻量全景渲染不能阻挡表单、导航和状态确认。

### 9.2 视觉方向

整体关键词：搞笑、潮流、年轻化、表情包感、游戏化、房间探索感，同时保留编辑感、高对比和购物小票感。

- 基础色：黑色 `#0B0B0F` 与白色 `#FAFAFA`，形成清晰的编辑式对比。
- 品牌强调色：电光紫 `#7C3AED`，用于主按钮、关键数字和转场重点。
- 辅助强调色：荧光青 `#22D3EE`，仅用于少量互动提示。
- 成功反馈色：活力黄 `#FACC15`，用于“确认省下”和回血进度。
- 信息层级色：黑、白、灰用于正文、次要说明、分割线和禁用状态；卡片深色 `#18181B` 用于深色区域内的信息分组。

- 字体：标题使用粗黑体或系统无衬线字体的重字重，形成短促、有冲击力的表达；正文使用清晰的系统无衬线字体。活动版不加载体积较大的远程字体。
- 图形：可使用贴纸、弹窗、警告条、购物小票、进度条和虚拟金币，形成表情包与轻游戏界面的感觉。
- 场景：房间使用用户提供的 2:1 场景图完成轻量球形全景，至少包含健身区消费测试屏、沙发茶几手机、预算白板、快递堆和一个常驻文字导航。
- 主 IP 与 AI 载体：小浣熊负责开屏、世界观与房间引导；健身区钱包病历本屏幕 / 诊疗终端承担 AI 解释与反馈。角色不得遮挡数据，也不得成为完成任务的前提。
- 数据反馈：房间物件可以根据真实记录变化，例如冷静中订单增加时电脑出现待处理提示，回血进度增加时存钱罐或植物逐渐变化。

图形和插画不得遮挡金额、状态或操作按钮，也不能让用户误以为网页正在进行真实支付。优先使用原创或明确可商用素材，不直接复制参考网站或现成表情包。

### 9.3 页面设计转译

| 页面 | 参考方向 | 必须保留的可用性 |
| --- | --- | --- |
| 首页 | 可环视全景房间、物件热点、镜头聚焦、数据驱动的场景变化 | 首屏直接看见产品说明、文字导航和有文字名称的功能入口 |
| 云下单 | 高对比字段分组、小票式金额反馈 | 表单不使用滚动特效，错误提示紧邻字段 |
| 结果页 | 小票揭示动画、金额数字动效 | “幸好没买”和“最终购买”始终清楚可见 |
| 钱包病历本 | 房间中的视觉主角、AI 诊疗终端、编辑式数据排版、大数字与短文案穿插 | 统计、AI 依据、建议和边界说明必须同时可见，不依赖动画或角色才能理解 |
| 回血计划 | 大号进度数字、纵向叙事和阶段反馈 | 目标金额、虚拟进度说明和操作入口固定可见 |
| 分享海报 | 海报化构图、强标题和留白 | 不复制参考站素材，不泄露商品和原因 |

桌面端可以使用 12 栏网格，移动端收敛为 4 栏或 8 栏逻辑。大字号用于建立节奏，但不得造成关键信息被截断。

### 9.4 响应式要求

- 最小支持宽度为 375px。
- 移动端为主要设计基准，主按钮应方便单手操作。
- 桌面端和移动端均可拖动环视全景；移动端同时提供固定底部导航，不要求用户必须拖拽或旋转镜头才能找到功能。
- 移动端房间无法同时容纳所有热点时，底部导航为权威入口，房间不得通过横向滚动强行展示。
- 桌面内容区域设置合理最大宽度，避免信息横向拉得过散。
- 375px、768px 和 1440px 宽度下不得出现横向滚动或内容遮挡。

### 9.5 基础可访问性

- 正文和背景颜色对比度目标不低于 4.5:1。
- 所有输入项具有可见标签和明确错误提示。
- 交互状态不能只依赖颜色表达，需同时提供文字或图标。
- 可点击区域不小于 44 × 44px。
- 键盘可以完成主要流程，并显示清晰焦点。
- 房间内的功能物件必须以语义化按钮或链接实现，具有可见名称、键盘焦点和非空闲文案，不能只依赖图像坐标点击。
- AI 生成中、成功、失败和降级状态需向辅助技术公布，但不得造成频繁重复播报。
- 除已确认的约 3 秒强制开屏外，动画不应阻止操作；用户偏好减少动态效果时，开屏保留相同时长但改用低位移的透明度 / 像素密度变化，其他非必要动画继续减少。

### 9.6 动效与性能边界

- 首屏固定使用一次约 3 秒的原创像素瀑布开场。动画期间不显示进入按钮、不提供跳过，也不开放拖动、滚轮或热点；动画结束后入口定格，点击进入才把同一全景镜头拉远并开放房间交互。
- 页面转场建议控制在 300 至 500ms；数字反馈动画建议不超过 600ms。
- 表单输入、错误提示、订单状态确认和导航不得依赖滚动触发。
- 动效优先使用 CSS 和轻量前端动画方案；只有明确提升核心体验时才使用 Canvas。
- P0 房间优先使用分层 SVG / CSS 实现。如技术原型证明 WebGL 对交互有必要价值，也必须在房间离开视口、页面进入后台或开启减少动效时停止渲染；不使用全屏视频或移动端自定义光标。
- 房间素材加载失败时，页面需直接显示文字导航和今日数据，不得白屏。
- 用户启用“减少动态效果”时，取消视差、位移和数字滚动，仅保留必要状态变化。
- 动画加载失败时，内容和操作仍需完整可用。

---

## 10. 核心文案

### 10.1 首页文案

- 今日钱包暂未阵亡。
- 你可以想买，但钱包建议你先演一下。
- 欢迎来到成年人自救中心。
- 今天也要努力不把工资变成奶茶。

### 10.2 下单成功文案

- 云下单成功，先别急着把它算成省下。
- 商家不会发货，这笔欲望正在冷静中。
- 记录完成，现在轮到你决定买还是不买。
- 你刚刚给钱包争取了一点思考时间。

### 10.3 确认省下文案

- 幸好没买，本次确认省下 ¥{amount}。
- 钱包回血进度 +¥{amount}，记得根据实际情况自行存钱。
- 这次不是忍住，是你重新决定了什么值得买。

### 10.4 分析文案

- 你的消费欲望主要流向了食物链顶端：外卖平台。
- 本周你对奶茶行业的关注略显真诚。
- 你不是爱购物，你只是太容易被“限时”两个字绑架。
- 小额消费正在用蚂蚁搬家的方式接近你的钱包。

### 10.5 AI 钱包助手文案

- 我先看数据，不随便给你贴标签。
- 这是根据你近 30 天自己记录的行为观察。
- 今天先做一件小事，不用突然过苦日子。
- 钱包病历本不是理财顾问，也不会替你做决定。

### 10.6 分享文案

- 我本周确认没买 ¥{amount}，给钱包争取了一次回血。
- 今天没有乱买，钱包夸我是人类高质量清醒。
- 我不是抠，我是在给未来的自己留预算。
- 别人下单等快递，我下单等自己冷静。

文案可以幽默，但不能羞辱用户、制造财务焦虑或暗示医疗诊断结论。

---

## 11. 技术方案

### 11.1 推荐实现

- 前端：当前活动版使用无构建的原生 ES Modules、语义化 DOM 与响应式 CSS；保持单文件级模块边界，避免为活动原型引入不必要的框架运行时。
- 评分：`personality-scoring.js` 在浏览器本地计算 7 / 30 天统计、五维、动机、行为结果、人格与置信度；相同输入和规则版本必须可复算。
- 房间场景：使用无重型框架依赖的轻量 WebGL 球形全景；纹理按设备能力降采样，热点和业务面板使用语义化 DOM。
- 场景工程：路由负责可分享的页面状态，场景状态机只负责镜头、焦点和环境反馈；三个一级物件使用统一热点注册表，同时驱动房间、文字导航和自动化测试。
- 数据：浏览器 LocalStorage，不接后端数据库。
- 后端：阿里云服务器运行 Node.js 20 及以上版本的同源服务，同时托管静态页面和 `/api/ai/diagnosis`，不设置业务数据库。
- AI 服务：模型供应商确定为 DeepSeek，默认使用 `deepseek-v4-flash` 的 Chat Completions JSON Output；后端负责 12 秒内超时、内存限流、输入长度校验、证据目录约束和输出 Schema 校验。模型名与 Base URL 通过环境变量配置，前端不依赖 DeepSeek 私有返回格式。
- 凭证：AI 模型 API 密钥只能保存在服务端环境变量中，不得出现在前端源码、构建产物或浏览器请求参数中。
- 海报：前端生成图片，可使用 Canvas 或成熟的图片导出方案。
- 导出：使用浏览器 Blob 和下载能力在本地生成 JSON、CSV 文件。
- 部署：阿里云 Linux 服务器使用 systemd 运行 Node 服务，由 Caddy 或 Nginx 提供 HTTPS 和同域反向代理。

### 11.2 浏览器范围

- 支持当前主流 Chrome、Safari 和 Edge 的最近两个大版本。
- 重点验证 iOS Safari 和 Android Chrome。
- 不保证无痕模式、禁用本地存储或极旧浏览器中的数据持久化。

### 11.3 质量目标

- 常见移动网络下，全景加载需要显示明确进度或降级状态；资源就绪后约 3 秒完成强制开屏并出现可操作的进入按钮。
- 页面刷新和直接访问主要入口时不出现白屏。
- 下单、订单状态、确认省下、本地统计、回血计划和数据导出不依赖 AI 或其他运行时外部服务。
- AI 请求的成功、超时、限流、无效结构和主动取消都需要有可验证的页面状态。
- 正式演示构建中不得出现阻断操作的控制台错误。

### 11.4 隐私与安全边界

- 不采集真实姓名、手机号、支付信息或银行信息。
- 商品名称和备注只保存在本地浏览器，默认不进入 AI 请求。购买原因只以次数汇总的形式进入 AI 请求。
- AI 请求前页面必须显示清晰的数据摘要；不同意不影响本地统计和其他核心功能。
- 服务端日志不得记录完整 AI 输入、完整 AI 输出或可还原单笔消费的数据；可仅记录请求 ID、耗时、结果状态和错误类型。
- 本版 AI 会话不用于训练个人模型，不建立跨用户画像，不将一名用户的数据暴露给其他用户。
- AI 模型供应商必须具有可查证的数据保留与训练政策，并能满足本项目确定的不用于模型训练和最小化保留要求；未完成这一验证前，不能宣称 AI 隐私边界已满足上线要求。
- 数据导出仅由用户主动触发，生成过程不得把记录发送给第三方。
- 未经用户明确同意，不接入统计脚本或第三方追踪服务。
- 所有用户输入按纯文本显示，禁止作为 HTML 直接渲染。

---

## 12. 建议两周开发安排

| 日期阶段 | 主要任务 | 完成标志 |
| --- | --- | --- |
| 第 1 天 | 冻结 PRD、AI 数据边界、房间导航逻辑和页面清单 | P0 范围无冲突 |
| 第 2 天 | 完成全景房间低保真原型、移动底栏和键盘导航原型 | 三个核心物件、快递探索区、诊疗终端、聚焦转场和返回逻辑可评审 |
| 第 3 天 | 完成 AI 请求 / 返回 Schema、隐私确认稿和本地规则降级原型 | 可以用演示数据完成真实 AI 请求与失败降级 |
| 第 4–5 天 | 完成首页、下单、订单状态和本地统计 | 非 AI 主流程可独立走通 |
| 第 6–7 天 | 完成 AI 钱包病历本、消费人格、引导式追问和回血计划 | AI 结果有数据依据，不自动修改业务数据 |
| 第 8 天 | 完成房间视觉、数据驱动物件反馈、分享海报和数据导出 | 空间化体验与业务数据已连通 |
| 第 9 天 | 完成响应式、减少动效、键盘操作、AI 故障注入和浏览器验证 | P0 验收项通过 |
| 第 10 天 | 部署、完整彩排、宣传视频和交付整理 | 获得公开链接、可用 AI 服务和演示脚本 |

外部路线图可作为协作补充，但不得替代本节的活动版范围和验收口径。

---

## 13. 验收标准

### 13.1 功能验收

1. 首次进入无需登录，能选择真实体验或加载演示数据。
2. 商品名为空、金额非法或未选分类/原因时，不能提交并显示对应提示。
3. 合法提交只生成一笔订单，初始状态为“冷静中”。
4. 选择“幸好没买”后，确认省下和目标进度按规则增加。
5. 选择“最终购买”后，该金额不得进入确认省下和目标进度。
6. 修改订单状态后，首页、分析和目标数据同步更新。
7. 删除订单后，相关统计同步扣除。
8. 刷新页面后，本地数据仍存在。
9. 加载演示数据后，可以立即查看 7 天和 30 天分析。
10. 重置数据需要二次确认，并能恢复首次进入状态。
11. 分享海报可以预览并保存，且不展示商品名称和购买原因。
12. JSON 导出包含当前个人订单、当前目标、版本号和导出时间。
13. CSV 导出字段完整，中文、逗号、换行和引号能够正确打开。
14. 默认导出不包含演示数据，导出过程不产生网络上传请求。
15. 没有个人数据时，导出按钮不可用并显示明确原因。
16. 至少有 3 笔订单时，用户可以主动发起 AI 钱包诊断；不足 3 笔时显示记录引导。
17. 首次 AI 请求前必须展示数据用途和发送摘要，只有用户明确同意后才产生网络请求。
18. AI 请求默认不包含商品名称和备注，服务端不向前端暴露模型 API 密钥。
19. AI 返回的消费人格、数据依据、行动建议和目标关联字段结构完整，且金额与次数不得与本地统计矛盾。
20. AI 超时、限流或输出无效时，页面明确标记降级状态，仍可查看本地统计、基础人格和固定规则建议。
21. AI 输出不能直接修改订单状态、删除订单、修改目标或产生真实资金操作。

### 13.2 页面验收

1. 375px、768px 和 1440px 宽度下无横向滚动、遮挡或无法点击的控件。
2. 移动端主流程能够单手完成，底部导航不会遮挡内容。
3. 键盘可以访问表单、主要按钮、导航和弹窗。
4. 输入错误、空状态、加载状态和确认弹窗均有明确文案。
5. 页面状态不能只通过颜色区分。
6. 桌面端房间至少展示健身区消费测试屏、沙发茶几手机和预算白板三个可交互区域，点击后会进入对应功能内容。
7. 房间物件均可使用键盘聚焦和触发，并有可见文字名称；不看插画也能通过文字导航完成主流程。
8. 点击物件后，功能内容可通过返回、浏览器后退或 `Esc` 关闭，并且 URL 与当前页面同步。
9. 移动端保留可拖动全景和固定底部导航，不需要依赖拖拽、旋转或缩放镜头才能找到功能。
10. 启用“减少动态效果”后，房间视差、镜头推近和循环装饰动画被移除，但业务内容与导航不受影响。

### 13.3 演示验收

1. 使用新浏览器环境，从房间首页到完成一次确认省下不超过 60 秒。
2. 演示数据、真实体验和重置流程互不混淆。
3. 使用演示数据时，从进入钱包病历本到获得一次完整 AI 诊断的目标时间不超过 30 秒。
4. 断开 AI 服务后，下单、订单状态、本地统计、回血计划、数据导出和本地规则降级仍可运行。
5. 演示前完成一次手机和桌面浏览器全流程彩排，并分别验证 AI 成功与 AI 降级两条路径。

### 13.4 完成状态说明

只有完成实现并通过上述验证后，才能标记为“活动版已完成”。

文档完成、代码完成、自动化通过、浏览器验证、部署完成和现场验收必须分别记录，不能互相替代。

---

## 14. 宣传视频方案

### 14.1 定位

- 类型：年轻人钱包自救短剧。
- 时长：约 30 秒。
- 风格：搞笑、快节奏、表情包、弹幕和潮流贴纸。
- 片名：《当代年轻人的钱包自救》。

### 14.2 视频脚本

| 时间 | 段落 | 画面 | 字幕/文案 | 音效 |
| --- | --- | --- | --- | --- |
| 0-3 秒 | 痛点开场 | 用户刷手机，外卖、奶茶和购物车提示连续出现 | 每次想省钱，手都比脑子快。 | 警报声 |
| 3-7 秒 | 冲动爆发 | 用户想点一杯 28 元奶茶 | 又想喝奶茶了。钱包：你礼貌吗？ | 节奏停顿 |
| 7-12 秒 | 产品出现 | 打开全景房间，点击沙发茶几手机或底部下单入口，输入奶茶和 28 元 | 别急，先云下单。 | 点击声 |
| 12-16 秒 | 记录成功 | 生成虚拟订单小票，状态为冷静中 | 商家不会发货，这笔欲望正在冷静中。 | 小票打印声 |
| 16-20 秒 | 用户决定 | 用户点击“幸好没买” | 本次确认省下 ¥28。 | 金币回血声 |
| 20-24 秒 | 目标变化 | 旅行基金进度增加 28 元 | 这是虚拟进度，记得真的存起来。 | 进度上涨声 |
| 24-27 秒 | AI 钱包诊断 | 点击健身区消费测试屏，显示“奶茶股东型选手”、数据依据和一步建议 | 我先看数据：本周你对奶茶行业的关注略显真诚。 | AI 诊断提示声 |
| 27-30 秒 | 结尾 | 产品 Logo 与 Slogan | 让你花个爽！假装下单，真的回血。 | 收尾音效 |

---

## 15. 后续版本方向

活动版完成并验证后，再根据真实反馈决定是否继续开发：

- 真实 24 小时冷静期和站内提醒。
- 多目标分配和长期趋势。
- 用户登录和云端同步。
- 更丰富的人格、建议风格和用户可选的 AI 助手人设。
- 在明确的隐私、安全和删除机制下，评估跨会话 AI 记忆和云端历史诊断。
- 空间交互按“P0 看起来能走进去 → P1 固定范围活动与靠近触发 → P2 多主题钱包小城”演进；每次升级都必须证明它提升功能发现、AI 使用或消费冷静行为，而不是只增加视觉成本。
- 钱包小城可以扩展诊疗所、订单工作站、目标储蓄区等主题空间，以及原创角色成长、任务和彩蛋，但不照搬参考站的城市、角色或内容。

后续功能不得反向扩大本次活动版 P0 范围。
