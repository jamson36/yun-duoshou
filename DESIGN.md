# 云剁手空间交互与 AI 系统设计

> 状态：设计基线，进入低保真原型后可按验证结果更新
> 模式：交互机制借鉴，不做 1:1 复刻
> 更新日期：2026-08-25
> 产品需求依据：[云剁手活动网页产品需求文档（PRD）](./云剁手活动网页产品需求文档%20(PRD).md)

## 0. 决策摘要

- **一句话体验**：用户进入一个代表自己钱包状态的 2.5D 房间，点击物件完成“记录欲望、处理订单、让 AI 看懂消费模式、推进目标”的闭环。
- **P0 场景方案**：分层 SVG / CSS 构成固定视角的 2.5D 房间，产品内容使用语义化 DOM 面板；不实现自由行走、碰撞、多人和持续 WebGL。
- **核心视觉主角**：中心诊疗桌 / 病历柜对应“钱包病历本”，亮度、构图和默认焦点均高于订单与目标区域。
- **AI 载体**：可使用原创小动物或会说话的诊疗终端。它必须承担解释、追问和反馈；没有 IP 时，诊疗桌屏幕承担同一职责。
- **第一条竖切链路**：进入房间 → 点击电脑 → 新建冷静中订单 → 标记“幸好没买” → 房间与统计更新 → 进入钱包病历本 → 同意发送汇总 → AI 返回结构化建议或本地降级。
- **数据边界**：订单、目标和 AI 结果保存在当前浏览器；唯一后端是无数据库的同源 AI 代理，不承载账户和业务数据存储。

### 0.1 本次实现的最低视觉交互门槛

本次交付不能退化为“在普通首页上摆四张功能卡”。首屏必须同时满足：

1. 一个占据主要视口、具有墙面、地面和景深层次的完整房间场景。
2. 一个始终可见、拥有空闲动作和状态反馈的原创主 IP；正式名称与最终造型可后续冻结。
3. 房间内恰好四件主要可交互物，分别对应云下单、订单、钱包病历本和回血计划。
4. hover、键盘聚焦或触摸提示能让用户明确物件可操作，并展示文字名称。
5. 点击物件后，房间必须先产生镜头推近、放大聚焦或等价的空间转场，再展开对应功能内容。
6. 关闭、`Esc` 或浏览器后退能够回到原房间视角，键盘焦点返回原物件。
7. 主 IP 会随当前物件和数据状态改变对话或动作，不能只是静态吉祥物。

以上七项是 P0 浏览器验收门；缺少任一项都不能标记“房间交互已完成”。

## 1. 参考来源与借鉴边界

| 参考 | 只研究什么 | 明确不复制什么 | 当前取证状态 |
| --- | --- | --- | --- |
| [basement.studio](https://basement.studio/) | 空间作为入口、物件进入内容层、镜头反馈 | 品牌、文案、素材、页面构图 | 产品方向参考 |
| [samsy.ninja](https://samsy.ninja/) | “走进一个数字空间”的整体体验、区域探索、空间内功能装置、角色与环境反馈 | 城市造型、角色、品牌、作品内容、文案和视听素材 | 已确认作为体验参考 |
| [samsyninja-rebuild](https://github.com/boyang-hu/samsyninja-rebuild) | 场景状态机、路由与场景同步、热点注册、事件桥，以及“镜像→逆向→溯源移植→里程碑验证→可读工程”的复刻流程 | 城市模型、贴图、音视频、角色、源码实现和品牌表达 | README、可读源码与逆向笔记已抽样核对 |
| [论坛讨论](https://linux.do/t/topic/2716188/22) | `design.md` 与复刻流程的方法线索 | 未验证正文中的任何具体结论 | 本轮连接超时，暂不作为事实依据 |

参考仓库的 README 明示其站点资产归原作者并仅供学习研究，且 GitHub 未显示可直接复用整仓代码的许可。因此本项目只提炼通用工程机制，所有视觉、代码和资产重新创作。

### 1.1 已确认的体验借鉴

云剁手需要同时吸收原站的空间体验与复刻工程的方法，而不是只借其中一层：

1. **房间就是界面**：用户不是先看到普通首页再进入功能，而是直接进入代表钱包状态的空间。
2. **物件就是功能**：电脑、诊疗桌和目标墙承担真实操作，不是只能观看的装饰。
3. **页面切换也是场景变化**：路由变化同时驱动镜头、焦点、灯光或局部环境反馈。
4. **角色承担交互职责**：AI 小动物或诊疗终端负责引导、解释、思考状态和追问，类似空间中的功能型 NPC。
5. **数据改变环境**：订单堆积、AI 待复诊和目标进度通过房间物件变化被感知。
6. **探索不能牺牲可用性**：P0 用点击和轻镜头建立“走进去”的感觉，同时保留文字导航、键盘和移动端入口。

空间能力按三阶段演进：

| 阶段 | 空间体验 | 产品边界 |
| --- | --- | --- |
| P0：看起来能走进去 | 2.5D 房间、入场镜头、物件热点、AI 角色/终端、数据反应 | 无自由移动和碰撞，先完成 AI 与本地业务闭环 |
| P1：真的能在房间活动 | 评估 Three.js 固定范围移动、靠近触发和更立体的镜头/灯光 | 必须先通过移动端、性能和功能发现性验证 |
| P2：扩展为钱包小城 | 多个主题空间、角色成长、任务和彩蛋 | 只有真实用户反馈证明探索能提升核心行为时才启动 |

## 2. 三种方案比较

| 方案 | 产品价值 | 成本与风险 | 决策 |
| --- | --- | --- | --- |
| A. 分层 SVG / CSS 2.5D + DOM 面板 | 有“进入房间”的感觉，加载轻，键盘与移动端容易保障 | 视角和深度有限，需要高质量原创插画 | **P0 采用** |
| B. Three.js 固定相机 + DOM 面板 | 镜头、灯光和物件状态更立体，仍可保留常规内容层 | 资产、渲染、兼容和性能成本上升 | P1 原型证明价值后再考虑 |
| C. 完整 3D 自由行走小城 | 探索感最强，可容纳角色和大量彩蛋 | 控制、碰撞、相机、建模、移动端和新手教学会吞噬首版工期 | P0 不采用 |

选择 A 的原因不是放弃“空间感”，而是把空间感用于导航和反馈。云剁手的关键结果是用户看懂自己的消费模式，不是学会操纵角色。

## 3. 场景构图与物件地图

```text
┌──────────────────── 钱包房间 ────────────────────┐
│  云下单打印机             电脑/订单区              │
│  记录一笔消费欲望         查看与处理冷静中订单      │
│                                                   │
│  中央诊疗区              目标墙/存钱罐            │
│  钱包病历本、AI、小动物   回血计划与进度反馈        │
│                                                   │
│             [ 我想买，先假买 ] 常驻主按钮          │
└───────────────────────────────────────────────────┘
```

| 物件 / 区域 | 产品含义 | 路由 / 操作 | 数据驱动变化 | 非空间入口 |
| --- | --- | --- | --- | --- |
| 云下单打印机 | 记录消费欲望 | `/orders/new` | 新订单生成时吐出一张冷静小票 | “云下单”主按钮 |
| 电脑 | 查看与处理订单 | `/orders` | 冷静中数量角标、屏幕小票堆叠 | “订单”导航项 |
| 中央诊疗桌 / 病历柜 | AI 钱包病历本 | `/clinic` | 数据足够时亮灯；AI 结果过期时显示待复诊 | “病历本”导航项 |
| 目标墙 / 存钱罐 | 回血计划 | `/goals` | 确认省下金额推动刻度、贴纸或植物成长 | “回血计划”导航项 |
| AI 小动物 / 诊疗终端 | 解释与引导，不是一级页面 | 聚焦诊疗区或发起引导式追问 | 空闲、思考、成功、降级四种状态 | 病历本内的文字按钮 |

中央诊疗区在构图中占据最大面积和最高对比度。订单区负责“记录”，目标区负责“反馈”，诊疗区负责“理解和行动”，三者不能抢同一视觉主级。

## 4. 状态与导航模型

### 4.1 路由状态

```text
/                 房间
/orders           订单列表
/orders/new       云下单
/clinic           钱包病历本
/goals            回血计划
```

直接访问任一路由都能恢复对应内容。浏览器后退回到上一个产品状态；从房间打开的内容层关闭后回到 `/`。桌面支持 `Esc` 关闭内容层，但表单有未保存输入时必须先确认。

### 4.2 场景状态

```text
BOOT → INTRO → IDLE → FOCUS_ORDERS / FOCUS_CLINIC / FOCUS_GOALS
                         ↓
                    PANEL_OPEN

任意状态 → REDUCED_MOTION
任意状态 → SUSPENDED（页面隐藏或场景离开视口）
```

- `INTRO` 不超过 800ms，可立即操作或跳过。
- `FOCUS_*` 只负责 300–500ms 的镜头/层级反馈，不能阻挡面板打开。
- `SUSPENDED` 停止循环动画和非必要计时器。
- 路由是可分享状态的权威来源；场景根据路由投影，不另存一套页面状态。

### 4.3 数据状态

- `empty`：无个人记录、无演示数据。
- `demo`：只有带 `demo` 标记的数据。
- `personal`：存在个人记录。
- `mixed`：个人与演示数据并存时，统计区必须明确当前数据范围。
- `storage_error`：本地存储不可用或写入失败，允许临时体验并提示无法持久保存。

### 4.4 AI 状态

```text
INELIGIBLE (<3 笔)
  → READY
  → CONSENT_REQUIRED
  → REQUESTING
  → SUCCESS
  → STALE（底层订单 revision 已变化）

REQUESTING → FAILED → FALLBACK
```

`FAILED` 和 `FALLBACK` 必须有显式标签，不能把规则文案伪装成模型回答。

## 5. 核心交互规格

### 5.1 首次进入

1. 首屏立即显示产品一句话、常驻下单按钮和三个带文字名称的物件。
2. 房间在 800ms 内完成层级归位；用户点击时立即中止入场并响应。
3. 一次性引导只指明“订单、钱包病历本、回血计划”，不教授游戏操作。
4. 提供“加载演示数据”，但必须说明是否覆盖当前数据。

### 5.2 点击物件

1. 物件 hover / focus 时出现名称和当前状态，例如“电脑：2 笔还在冷静中”。
2. 激活后房间轻微推近，内容面板同时进入，不等镜头结束。
3. URL 与内容同步；键盘焦点移动到面板标题，关闭后回到原物件。
4. 图片层失效时仍显示今日数据、文字导航和常驻下单按钮。

### 5.3 数据反应

- 创建冷静中订单：电脑出现新小票，诊疗桌不把它算成“省下”。
- 标记幸好没买：存钱罐/目标墙产生一次短反馈，确认省下与目标进度重算。
- 标记最终购买：电脑归档订单，不触发“回血”庆祝。
- 删除或纠正状态：所有数字和场景状态从订单源记录重算，不做增量猜测。
- 订单变化后：旧 AI 结果显示“数据已变化，建议重新分析”。

### 5.4 AI 交互

首次 AI 诊断是主体验，不做开放式聊天首页。病历本先呈现本地事实，再出现“请它看看”。用户能看到将发送的汇总范围，并可不同意；不同意时仍获得本地人格和规则建议。

AI 首次结果固定为：消费人格、两条数据依据、模式解读、一步建议、目标关联和边界说明。之后最多选择 3 个引导式问题、完成 3 轮追问。

## 6. 系统架构

```text
React Router ───────────────► 页面 / 内容层
      │                            │
      ▼                            ▼
场景状态适配器 ◄──── 事件桥 ──── 产品 Store
      │                            │
      ▼                            ├──► 确定性统计与人格规则
2.5D 场景 + 热点注册表             ├──► LocalStorage 版本化存储
                                   ├──► JSON / CSV 本地导出
                                   └──► AI 汇总请求
                                             │
                                      同源 Serverless 代理
                                             │
                                      模型适配 + Schema 校验
```

### 6.1 前端模块边界

- `app/router`：URL、直接访问、后退和内容层入口。
- `domain/orders`：订单校验、状态机和状态历史。
- `domain/analytics`：7/30 天统计、人格规则、目标进度，纯函数可复算。
- `domain/storage`：数据 envelope、迁移、演示数据、重置和异常恢复。
- `features/room`：场景层、热点注册表、焦点与数据反应，不保存业务事实。
- `features/clinic`：同意、AI 状态机、结构校验、过期判断和降级。
- `features/export`：JSON/CSV，本地 Blob，不产生网络请求。

### 6.2 热点注册表

三个一级物件共用一个配置源，供场景按钮、底部导航和自动化测试使用：

```ts
type HotspotId = 'orders' | 'clinic' | 'goals'

type Hotspot = {
  id: HotspotId
  label: string
  route: string
  sceneTarget: string
  priority: 'primary' | 'secondary'
  getStatusText(view: ProductView): string
}
```

避免在 SVG 图层、移动导航和桌面文字导航中分别维护链接，导致名称、顺序或状态不一致。

## 7. 本地数据与导出

P0 使用一个版本化 LocalStorage envelope：

```ts
type LocalEnvelopeV1 = {
  schemaVersion: 1
  dataRevision: number
  orders: Order[]
  currentGoal: Goal | null
  settings: {
    onboardingSeen: boolean
    aiConsent: boolean
    reducedMotionOverride?: boolean
  }
  latestAiResult?: {
    sourceRevision: number
    period: 7 | 30
    generatedAt: string
    result: ValidatedAiResult
  }
}
```

- 任何订单或目标变化都递增 `dataRevision`。
- `sourceRevision !== dataRevision` 时，AI 结果过期。
- 所有金额、排名和进度均由 `orders` 与 `currentGoal` 重新计算。
- 演示数据保留 `demo: true`，默认不进入个人导出。
- JSON 完整备份和 CSV 订单表在浏览器本地生成；CSV 防止以 `= + - @` 开头的输入被表格软件执行。
- 存储不可用时保留内存会话并明确提示“刷新后可能丢失”，不能静默假装已保存。

## 8. AI 请求与输出契约

### 8.1 前端发送的最小汇总

```ts
type WalletSummaryRequest = {
  period: 7 | 30
  totals: {
    recorded: number
    saved: number
    purchased: number
    coolingCount: number
  }
  categories: Array<{ category: string; count: number; amount: number }>
  reasons: Array<{ reason: string; count: number }>
  goal?: { name: string; targetAmount: number; progress: number }
  demo: boolean
}
```

默认不发送商品名、备注、导出文件、浏览历史和真实身份。前端 API 密钥为零；真实模型请求只通过同源代理发出。

### 8.2 结构化结果

```ts
type ValidatedAiResult = {
  persona: { title: string; summary: string }
  evidence: Array<{ metric: string; statement: string }>
  pattern: string
  action: { title: string; steps: string[] }
  goalLink: string
  disclaimer: string
  suggestedQuestions: string[]
}
```

服务端限制字段长度和数量；前端再次进行 Schema 校验。金额或次数与本地事实矛盾时，不展示该结果，转入本地降级。AI 无权修改订单、目标、同意状态或导出文件。

### 8.3 最小后端

Serverless / Edge 代理只负责：读取服务端密钥、限流、输入尺寸校验、模型适配、12 秒超时、输出 Schema 校验和必要的错误码映射。它不创建用户账户、不保存历史会话、不记录完整输入输出。

## 9. 响应式、可访问性与减少动态

| 宽度 / 模式 | 房间 | 内容层 | 导航 |
| --- | --- | --- | --- |
| 375px | 固定视角、简化装饰、热点不重叠 | 全屏页面 | 固定底部导航为权威入口 |
| 768px | 固定视角，可保留少量层差 | 全屏或大抽屉 | 底部导航 + 房间热点 |
| 1440px | 完整房间切面、轻视差和推近 | 右侧面板或全屏内容层 | 房间热点 + 文字导航 |
| 减少动态 | 无视差、无位移推近、无循环动画 | 直接切换 | 所有功能不受影响 |

- 每个热点必须存在语义化按钮或链接、可见文字、清晰焦点和至少 44×44px 的触控面积。
- 内容面板打开后焦点进入标题，关闭后回到触发物件。
- AI 加载、成功、错误和降级使用克制的状态播报。
- 颜色不是唯一状态线索；“冷静中、幸好没买、最终购买”均有文字与图标。
- 房间不是唯一导航。插画、SVG 或动画加载失败后主流程仍可完成。

## 10. 性能与韧性预算

- 常见移动网络下 3 秒内可操作；文字、数字和主按钮先于装饰资产出现。
- 不设置必须等待的开场；首屏场景入场不超过 800ms。
- 页面转场 300–500ms，状态数字反馈不超过 600ms。
- P0 不加载大体积 GLB、全屏视频、远程字体或持续音频。
- 循环动画在页面隐藏、场景离开视口和减少动态模式下停止。
- 单个场景资产 404 时显示文字导航，不白屏。
- AI 不可用时，本地订单、统计、目标、导出和规则建议全部可用。

## 11. 验证矩阵

| 场景 | 视口 | 输入 | 数据 | 网络 | 预期证据 |
| --- | --- | --- | --- | --- | --- |
| 首次进入房间 | 375 | 触控 | 空 | 在线 | 首屏截图、3 秒内可操作 |
| 电脑 → 下单 → 确认省下 | 1440 | 键盘 | 个人 | 在线 | 完整录屏/步骤截图、数字重算 |
| 房间热点与后退 | 1440 | 键盘 | 演示 | 在线 | 焦点顺序、URL、Esc、后退均通过 |
| AI 首次同意与成功 | 1440 | 指针 | 演示 | 在线 | 发送摘要、请求字段断言、结果截图 |
| AI 超时与无效结构 | 375 | 触控 | 个人 | 故障注入 | 明确降级、本地功能继续可用 |
| 刷新与直接 URL | 768 | 键盘 | 个人 | 在线 | 数据保留、页面可恢复 |
| JSON / CSV 导出 | 375/1440 | 触控/键盘 | 含特殊字符 | 离线 | 文件可解析、无上传请求、CSV 安全 |
| 减少动态 | 768 | 键盘 | 个人 | 在线 | 无视差/位移，主流程通过 |
| 场景资产失败 | 375 | 触控 | 空 | 资产 404 | 文字导航出现、无白屏 |

文档完成、代码实现、自动化通过、浏览器验证、部署、真实模型服务验证必须分别记录。

## 12. 里程碑

1. **M0 设计基线**：冻结本文件、热点地图、AI Schema 和资产边界。
2. **M1 空间壳竖切**：房间、主 IP、四个热点、URL、聚焦/返回、移动底栏和减少动态。
3. **M2 本地业务闭环**：下单、订单状态、统计、目标、本地存储和导出。
4. **M3 钱包病历本**：本地人格、AI 同意、成功、过期、错误和降级。
5. **M4 数据反应与视觉完成**：场景物件随订单和目标变化，补原创 IP / 终端反馈。
6. **M5 浏览器证据与部署**：375/768/1440、键盘、移动端、故障注入、生产构建和演示彩排。

## 13. 已登记偏差与暂不决定项

| 项目 | 参考机制 | 云剁手决定 | 原因 | 重新考虑条件 |
| --- | --- | --- | --- | --- |
| 空间移动 | 参考项目允许自由行走、碰撞和相机跟随 | P0 固定视角点击热点 | 首版核心是消费理解，不是移动玩法 | 用户测试证明探索本身显著提升完成率 |
| 渲染技术 | 参考项目使用复杂 WebGPU / Three 场景 | P0 分层 SVG / CSS | 更轻、更易访问和验证 | 轻量方案无法表达已冻结的关键视觉反馈 |
| 多人 | 参考项目含实时玩家 | P0 无账户、无多人 | 与本地私密消费记录冲突且无核心价值 | 未来出现明确的匿名共同挑战需求 |
| IP 形象 | 可用小动物承担 AI 人格 | 形态未冻结，职责已冻结 | 先验证 AI 交互，再定造型 | 低保真测试完成后选择原创方向 |

## 14. 待在低保真原型中验证的问题

1. 中央诊疗区能否在 10 秒内被理解为主功能，而不是装饰。
2. 内容层使用右侧面板还是全屏层，哪种更适合 375px 与 1440px 共用信息层级。
3. 小动物是否真正帮助理解 AI，还是诊疗终端已经足够。
4. 三种数据反应中，哪些能帮助理解状态，哪些只是噪声。

这些问题不阻塞 M1 场景壳；M1 使用无角色的诊疗终端作为默认占位，不提前绑定 IP 造型。
