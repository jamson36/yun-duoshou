# 让你花个爽！空间交互与 AI 系统设计

> 状态：设计基线，进入低保真原型后可按验证结果更新
> 模式：交互机制借鉴，不做 1:1 复刻
> 更新日期：2026-09-02
> 产品需求依据：[让你花个爽！活动网页产品需求文档（PRD）](./让你花个爽！活动网页产品需求文档%20(PRD).md)

## 0. 决策摘要

- **一句话体验**：用户进入一个可以左右环视的钱包全景房间，点击真实场景中的屏幕、手机和白板完成“看懂消费、模拟下单、处理订单、制定预算”的闭环。
- **P0 场景方案**：把用户提供的 2:1 全景图映射到轻量 WebGL 球面，使用统一热点注册表投影交互位置，产品内容继续使用语义化 DOM 面板；不实现自由行走、碰撞、多人和完整三维建模。
- **核心视觉主角**：健身区竖屏对应“消费测试 / 钱包病历本”，镜头聚焦、热点层级和默认引导均高于其他区域。
- **主 IP 与 AI 分工**：小浣熊是房间的主 IP，负责开屏、空间引导与情绪反馈；AI 仍由健身区的钱包病历本屏幕 / 诊疗终端承担解释、追问和状态反馈。角色不能替代文字导航，也不能成为完成任务的前提。
- **第一条竖切链路**：进入房间 → 点击沙发茶几手机 → 新建冷静中订单 → 标记“幸好没买” → 房间与统计更新 → 进入消费测试屏 → 同意发送汇总 → AI 返回结构化建议或本地降级。
- **数据边界**：订单、目标和 AI 结果保存在当前浏览器；唯一后端是无数据库的同源 AI 代理，不承载账户和业务数据存储。
- **开屏与入口**：普通访问先播放约 3 秒一次性开场，再自动进入 Figma 节点 `4:5` 对应的全屏循环等待画面；等待页只显示一个“开始”入口。进入房间后才显示顶部三个语义功能入口和房间热点，不显示可见底部导航。
- **订单事实源**：商城购物车图标就是订单入口，不建立独立购物车；手动添加、商城“我的商品”和订单列表都从同一份订单数据派生。
- **活动层**：右侧掌机承载“欲望剥壳机”，把从底部抛起的商品外层促销话术剥开、保留商品本体；它是可关闭的房间小游戏，不增加第四个一级导航，不保存分数或替用户做消费决定。
- **目标与报告**：同时只有一个当前确认省下目标；规则人格保持 9 类加“欲望观察员”，Figma 20 张人格图只作为可复现映射后的视觉卡。

### 0.1 2026-09-01 已确认交互合同

以下是已获用户确认的目标状态，不代表代码、自动化、浏览器或生产环境已经完成：

1. 桌面端和移动端进入房间后均移除可见底部导航，顶部固定展示“消费人格测试、开始买吧、回血计划”三个语义按钮；房间内三个热点继续保留。
2. 开屏保留“约 3 秒一次性开场 → Figma 节点 `4:5` 全屏循环等待”的双阶段结构；循环等待页只有一个“开始”入口。帮助、导入演示数据等辅助操作只能在进入房间后弱化展示，不得与开始按钮争夺层级。
3. “开始买吧”在桌面端使用背景承托仿真手机，在移动端直接使用同一手机内容。手机中的购物车图标打开订单，不另建购物车事实源。
4. 订单底层状态仍为“冷静中 / 幸好没买 / 最终购买”，界面分别显示“欲望冷却中 / 回血中 / 已剁手”；“已剁手”仍可纠正。
5. 商品名称、价格和图片来自用户输入或版本化的本地可复现商品池；AI 只生成建议文字，不能新增或覆盖订单事实。
6. 规则引擎输出 9 类人格或“欲望观察员”，再映射到 Figma 的 20 张视觉卡。报告商品区按金额取最多三件“已剁手”订单，不足三件时如实少显示。
7. 报告历史能够重新打开当次报告。测试完成后只设置一次性的刷新回房间并跳过开屏状态，普通重新访问仍显示开屏。
8. 月度目标只有一个当前值；滑块连续改变数值，并按四个 Figma 代表值切换 IP 形象与文案状态。进度继续由订单确定性重算。
9. 保存手动商品即创建一笔“欲望冷却中”订单；“我的商品”、订单入口、统计、报告和目标从该订单源同步更新。

### 0.2 本次实现的最低视觉交互门槛

本次交付不能退化为“在普通首页上摆四张功能卡”。首屏必须同时满足：

1. 一个占据主要视口、具有墙面、地面和景深层次的完整房间场景。
2. 健身区的钱包病历本屏幕 / 诊疗终端始终可识别，并能呈现空闲、分析中、成功、结果过期和降级状态。
3. 房间内至少有三个核心功能热点：健身区屏幕、沙发区手机和白板；快递堆提供可扩展的探索型热点。
4. hover、键盘聚焦或触摸提示能让用户明确物件可操作，并展示文字名称。
5. 点击物件后，房间必须先产生镜头推近、放大聚焦或等价的空间转场，再展开对应功能内容。
6. 关闭、`Esc` 或浏览器后退能够回到原房间视角，键盘焦点返回原物件。
7. 诊疗终端与场景物件会随订单、目标和 AI 状态改变提示、灯光或局部动作，不能只是静态装饰。

以上七项是 P0 浏览器验收门；缺少任一项都不能标记“房间交互已完成”。

## 1. 参考来源与借鉴边界

| 参考 | 只研究什么 | 明确不复制什么 | 当前取证状态 |
| --- | --- | --- | --- |
| [basement.studio](https://basement.studio/) / [官方公开仓库](https://github.com/basementstudio/website-2k25) | 空间作为入口、可检查物件、镜头反馈、物件拖拽 / 旋转与归位 | 品牌、文案、素材、页面构图与源码实现 | 已核对公开源码快照 `d515c224`；仓库根目录未发现许可证，仅研究机制 |
| [samsy.ninja](https://samsy.ninja/) | “走进一个数字空间”的整体体验、区域探索、空间内功能装置、角色与环境反馈 | 城市造型、角色、品牌、作品内容、文案和视听素材 | 已确认作为体验参考 |
| [samsyninja-rebuild](https://github.com/boyang-hu/samsyninja-rebuild) | 场景状态机、路由与场景同步、热点注册、事件桥，以及“镜像→逆向→溯源移植→里程碑验证→可读工程”的复刻流程 | 城市模型、贴图、音视频、角色、源码实现和品牌表达 | 已完整浅克隆并核对状态机、相机、射线交互、碰撞与城市装配源码；只借机制 |
| [论坛讨论](https://linux.do/t/topic/2716188/22) | `design.md` 与复刻流程的方法线索 | 未验证正文中的任何具体结论 | 本轮连接超时，暂不作为事实依据 |

参考仓库的 README 明示其站点资产归原作者并仅供学习研究，且 GitHub 未显示可直接复用整仓代码的许可。Basement 官方公开仓库根目录也未发现许可证。因此本项目只提炼通用工程机制，所有视觉、代码和资产重新创作。

### 1.1 已确认的体验借鉴

让你花个爽！需要同时吸收原站的空间体验与复刻工程的方法，而不是只借其中一层：

1. **房间就是界面**：用户不是先看到普通首页再进入功能，而是直接进入代表钱包状态的空间。
2. **物件就是功能**：手机、诊疗屏、白板和快递箱承担真实操作，不是只能观看的装饰。
3. **页面切换也是场景变化**：路由变化同时驱动镜头、焦点、灯光或局部环境反馈。
4. **角色与终端各司其职**：小浣熊负责世界观和引导，钱包病历本屏幕负责 AI 解释、思考状态和追问；任一层失效都不影响另一层完成任务。
5. **数据改变环境**：订单堆积、AI 待复诊和目标进度通过房间物件变化被感知。
6. **探索不能牺牲可用性**：P0 用点击和轻镜头建立“走进去”的感觉，同时保留键盘可用的顶部三个语义入口；桌面和移动端都不再使用可见底部导航。

空间能力按三阶段演进：

| 阶段 | 空间体验 | 产品边界 |
| --- | --- | --- |
| P0：可以环视和聚焦 | 球形全景、拖拽环视、物件热点、诊疗终端、镜头聚焦和数据反应 | 无自由移动和碰撞，先完成 AI 与本地业务闭环 |
| P1：物件层更真实 | 补充透明轮廓遮罩、独立物件图层、局部动画和更精细的光效 | 由分层源文件与首轮浏览器反馈决定优先级 |
| P2：扩展为钱包小城 | 多个主题空间、角色成长、任务和彩蛋 | 只有真实用户反馈证明探索能提升核心行为时才启动 |

### 1.2 Basement 可检查物件机制清单

| 表面 | 触发 | 状态变化 | 用户反馈 | 可复用原则 | 项目决定 |
| --- | --- | --- | --- | --- | --- |
| 可检查物件 | hover / 键盘聚焦 | `idle → discoverable` | 放大光标、名称或轮廓提示 | 先让用户知道“这个东西能拿起来看” | 采用语义热点、文字名称和原创光效 |
| 可检查物件 | 点击 | `discoverable → selected` | 物件以弹簧感离开场景、朝向镜头并统一到可读尺寸 | 先用空间动作确认选择，再展示内容 | 转译为手机浮起、报告吐出、白板转正等区域专属动作 |
| 已选物件 | 拖动 | `selected → manipulating` | 可旋转或偏移，松手后回弹 / 归位 | 让检查过程保留直接操控感 | 仅用于非表单物件；手机表单和白板文字继续由 DOM 操作 |
| 已选物件 | 关闭、切场景或滚动离开 | `selected → returning → idle` | 物件回到原位，场景重新可操作 | 退出动作必须与进入动作成对 | 关闭、`Esc`、浏览器后退统一归位并恢复焦点 |
| 关键选择 | 点击 / 完成 | `active → confirmed` | 克制的局部声效与视觉确认 | 声音是附加反馈，不能成为唯一反馈 | 音效后置且默认可关闭；P0 先完成视觉、文字与触觉等价反馈 |

以上仅是对公开实现的机制归纳，不代表获得了其代码或资产的复用许可。

## 2. 三种方案比较

| 方案 | 产品价值 | 成本与风险 | 决策 |
| --- | --- | --- | --- |
| A. 分层 SVG / CSS 2.5D + DOM 面板 | 加载轻，键盘与移动端容易保障 | 无法正确呈现 2:1 全景，也缺少环视和空间聚焦 | 已完成旧竖切，不作为最终场景 |
| B. 球形全景 WebGL + DOM 面板 | 直接使用最终构图，支持环视、镜头聚焦和世界坐标热点 | 需要同步两层状态并控制纹理与渲染预算 | **P0 采用** |
| C. 完整 3D 自由行走小城 | 探索感最强，可容纳角色和大量彩蛋 | 控制、碰撞、相机、建模、移动端和新手教学会吞噬首版工期 | P0 不采用 |

选择 B 是因为用户确认的 Figma 节点 `32:3631` 房间场景可派生为 2:1 全景素材；球形投影能保留它的空间意图，又不会引入完整三维建模、角色物理和碰撞成本。首页房间必须保留坐在红色凳子上的小浣熊。

## 3. 场景构图与物件地图

```text
左侧健身区 ── 中左快递堆 ── 中央沙发与茶几 ── 右侧书桌/白板 ── 右侧掌机
消费测试屏      随机内心 OS      手机：下单+订单        预算计划表单       欲望剥壳机
```

| 物件 / 区域 | 产品含义 | 路由 / 操作 | 数据驱动变化 | 非空间入口 |
| --- | --- | --- | --- | --- |
| 健身区竖屏 | 消费测试、总结、人格标签与分享海报 | `/clinic` | 数据足够时亮灯；结果过期时显示待更新 | “消费测试”导航项 |
| 沙发区茶几手机 | 模拟下单与订单处理的双页签设备界面；购物车图标语义为订单入口 | `/orders/new`、`/orders` | 冷静中数量角标、创建后短暂脉冲 | “开始买吧”顶部入口 |
| 白板 | 单个当前确认省下目标与连续四态预算滑块 | `/goals` | 当前目标进度、IP 形象和状态文案联动 | “回血计划”顶部入口 |
| 快递堆 | 探索型消费内心 OS，不占一级页面 | 无路由；悬停、聚焦或轻点 | 每次进入随机选择若干箱子和短文案 | “看看快递”场景定位按钮 |
| 右侧掌机 | 欲望剥壳机；辨认商品外层的催促话术 | `#room?activity=peel`；打开活动弹层 | 聚焦阶段只读取最近一笔非演示冷静单快照；不写订单 | 无第四个顶部入口，仅保留房间热点 |
| 钱包病历本屏幕 / AI 诊疗终端 | 解释与引导，不是独立一级页面 | 聚焦健身区屏幕或发起引导式追问 | 空闲、思考、成功、过期、降级五种状态 | 病历本内的文字按钮 |

健身区屏幕负责“理解”，沙发手机负责“记录与处理”，白板负责“规划”，快递堆负责“情绪共鸣”。三个核心功能必须通过顶部语义按钮直接可达，快递堆只增强探索感。

## 4. 状态与导航模型

### 4.1 路由状态

产品语义层级为“房间 → 功能首页 → 目录/详情或测试结果 → 报告 → 分享”。当前低依赖实现使用以下 Hash 状态：

```text
#room                                     房间
#new                                      “开始买吧”说明页
#new?view=home&type=shop                   模拟手机首页
#new?view=catalog&type=shop                模拟商城目录
#new?view=detail&type=shop&product=<id>     模拟商品详情
#orders                                   已下单订单
#clinic                                   人格扭蛋机
#clinic?view=report                        可滚动消费报告
#goals                                    当前目标
#goals-controller                         四态欲望控制器
#room?activity=peel                        房间内欲望剥壳机入口
```

测试结果、报告历史选择和分享卡当前是可访问弹窗或报告内状态，并非独立深链；如后续把它们提升为 `/clinic/result`、`/clinic/history`、`/clinic/share`，需同时补充路由恢复和浏览器返回测试。商城 URL 只保存 `view / type / product` 展示状态，不保存订单或评分事实。`activity=peel` 只恢复小游戏入口，每次打开都从 `READY` 或首次教学开始，不把计时、分数、命中目标或体感状态写进 URL。浏览器后退、页面返回、移动设备返回和 `Esc` 遵守现有页面层级；直接深链详情页时，页面内返回先回到同类目录，不把用户带离站点。从房间打开的功能首页关闭后回到 `#room`，小游戏和弹窗关闭后焦点恢复到原触发控件。报告历史项目重新打开保存的当次快照，不重新随机计算。

普通访问和普通深链仍按 URL 恢复内容。只有消费测试完成后设置一次性的 `skipIntroOnce`：紧接着的刷新清除测试瞬态、回到房间并跳过一次开屏；该标记消费后，下一次普通访问仍显示开屏。

### 4.2 场景状态

```text
BOOT → ASSET_LOADING → INTRO_CLIP → ENTRY_LOOP → ENTERING → IDLE
                                                        ↓
                              FOCUS_AREA → INSPECT_OBJECT → PANEL_OPEN / INTERACTING
                                                                  ↓
                                                          COMMIT_FEEDBACK
                                                                  ↓
                                                               RETURN → IDLE

任意状态 → REDUCED_MOTION
任意状态 → SUSPENDED（页面隐藏或场景离开视口）
```

- `ASSET_LOADING` 预载开屏视频、静态封面和房间全景；任一视频失败时立即切换清晰静态封面，不能白屏或阻断开始入口。
- `INTRO_CLIP` 播放约 3 秒一次性开场片段；它与随后的 `ENTRY_LOOP` 均铺满视口，不得出现视频与背景分层、露边或尺寸变化。
- `ENTRY_LOOP` 使用 Figma 节点 `4:5` 对应画面自动静音循环，只呈现一个“开始”入口；不显示三个功能入口、帮助、设置或导入演示数据。
- `ENTERING` 点击“开始”后在约 420ms 内自然过渡到默认房间视角，结束后才显示热点、顶部语义入口和房间业务状态。
- `FOCUS_AREA` 与 `INSPECT_OBJECT` 合计以 300–500ms 为目标，不能阻挡表单和主要操作出现。
- 同一时间只允许一个区域处于 `INSPECT_OBJECT`、`PANEL_OPEN` 或 `INTERACTING`，切换区域时先结束上一物件的悬浮 / 高亮状态。
- `COMMIT_FEEDBACK` 只表现已完成的数据动作，例如小票进入订单箱或便签固定；失败时不得播放成功反馈。
- `SUSPENDED` 停止循环动画和非必要计时器。
- 路由是可分享状态的权威来源；场景根据路由投影，不另存一套页面状态。

房间活动层使用独立、可丢弃的状态机：

```text
READY → TUTORIAL → PLAYING → SUMMARY
                    ↕
                  PAUSED
任意活动状态 → CLOSED → 房间 IDLE
```

首次教学商品也从底部抛起，在切割区悬停且不计时；正式局为 45 秒。页面隐藏、手势丢失或离开活动时暂停，关闭时清理动画帧、轨迹、粒子和摄像头输入。活动状态不进入 LocalStorage，不改变订单、目标、评分或报告 revision。

### 4.3 数据状态

- `empty`：无个人记录、无演示数据。
- `demo`：只有带 `demo` 标记的数据。
- `personal`：存在个人记录。
- `mixed`：个人与演示数据并存时，统计区必须明确当前数据范围。
- `storage_error`：本地存储不可用或写入失败，允许临时体验并提示无法持久保存。

### 4.4 AI 状态

```text
INELIGIBLE (<3 笔)
  → READY
  → CONSENT_REQUIRED
  → REQUESTING
  → SUCCESS
  → STALE（底层订单 revision 已变化）

REQUESTING → FAILED → FALLBACK
```

`FAILED` 和 `FALLBACK` 必须有显式标签，不能把规则文案伪装成模型回答。

## 5. 核心交互规格

### 5.1 首次进入

1. 预载约 3 秒全屏一次性开场片段、Figma 节点 `4:5` 对应的循环入口片段、静态封面和房间全景；视频不可播放时直接显示清晰静态封面。
2. 约 3 秒开场片段结束后无缝进入静音循环入口画面；媒体与背景必须保持同一裁切和定位，不能出现明显分层、露边或跳位。
3. 开屏只显示一个“开始”入口；帮助、导入演示数据和三个功能入口不得进入该页面的视觉或焦点顺序。
4. 点击开始后自然进入可左右环视的房间，再显示三个房间热点和顶部三个语义功能入口。
5. 帮助与“导入演示数据”只在房间中弱化展示；它们不得形成底部导航或与三个核心入口争夺主层级。

### 5.2 点击物件

1. 物件 hover / focus 时出现名称和当前状态，例如“手机：2 笔还在冷静中”。
2. 激活后房间轻微推近，并播放该区域专属的物件动作；内容同时进入，不等待动画结束。
3. URL 与内容同步；键盘焦点移动到面板标题，关闭后回到原物件。
4. 图片层失效时仍显示今日数据、顶部三个语义入口和主要业务按钮。

### 5.3 四个区域的专属交互

| 区域 | 进入 / 选中动作 | 核心操作形态 | 完成反馈 |
| --- | --- | --- | --- |
| 沙发手机：假装下单 | 镜头靠近茶几，手机从桌面轻抬并旋转到正面，屏幕从待机态亮起 | 手机式表单；字段可表现为对话气泡或逐步小票，但真实输入控件保持 DOM | 提交后生成一张虚拟小票，小票飞入“订单收件箱”；明确提示尚未计入确认省下 |
| 沙发手机：订单 | 手机切换到“订单”页签，桌面展开为一叠可检查小票 | hover / focus 抬起单张小票；打开后查看详情并选择“幸好没买”或“最终购买” | 前者盖绿色章并滑入“确认省下”档案盒；后者盖橙红章并滑入“最终购买”档案盒 |
| 健身区屏幕：钱包病历本 | 屏幕扫描光从上到下扫过，关键指标依次点亮，随后吐出一张“钱包体检报告” | 本地统计、消费人格、数据依据和 AI 建议在诊疗终端中分层展开；AI 请求显示明确的思考 / 失败 / 降级状态 | 消费人格以原创诊断标签出现；订单变化后报告盖“数据已变化”章并提示复诊 |
| 白板：预算计划 | 镜头将白板校正为近正视角，当前目标便签出现可落笔提示 | 编辑单个当前目标；连续滑块按四个代表状态切换金额、IP 形象与文案，表单输入仍是 DOM | 保存时以胶带 / 图钉压下固定，目标进度线随确认省下金额增长 |
| 快递堆：内心 OS | hover / focus 时箱体轻晃、胶带翘起；点击后箱盖打开或局部前移 | 随机弹出商品轮廓与一句内心 OS，例如“三分钟热度神器”；一次只突出一个箱子 | 关闭后箱盖归位；获得分层素材后可增加拖入“后悔区 / 真香区”的分类玩法 |

共同约束：所有空间进入动作都以 300–500ms 为目标；同一时间只激活一个区域；表单、订单详情和 AI 结果使用语义化 DOM，不绘制进 WebGL；启用“减少动态效果”时，浮起、飞入、扫描和旋转替换为淡入、状态章和短距离位置变化。

### 5.4 数据反应

- 创建冷静中订单：手机订单收件箱出现新小票，诊疗终端不把它算成“省下”。
- 标记幸好没买：存钱罐/目标墙产生一次短反馈，确认省下与目标进度重算。
- 标记最终购买：小票进入最终购买档案盒，不触发“回血”庆祝。
- 删除或纠正状态：所有数字和场景状态从订单源记录重算，不做增量猜测。
- 订单变化后：旧 AI 结果显示“数据已变化，建议重新分析”。

### 5.5 开始买吧与订单

1. PC 从房间进入后先显示 Figma 背景承托的仿真手机；手机端省略外围背景与设备壳，直接使用同一套手机内容。
2. 手机首页的购物、外卖、兴趣三个卡片进入各自商品目录和详情。商品名、价格和图片只能来自用户输入或版本化的本地商品池。
3. 保存手动商品或在商城模拟下单都创建一笔底层状态为 `冷静中` 的订单，并按标签归入购物、外卖或兴趣。“我的商品”、购物车图标打开的订单页、统计和报告从该订单源派生。
4. 不建立独立购物车状态。购物车图标的产品语义就是“查看已经模拟下单的订单”。
5. 用户可见状态映射为 `冷静中 → 欲望冷却中`、`幸好没买 → 回血中`、`最终购买 → 已剁手`；映射只影响文案，“已剁手”仍可纠正。
6. 下单反馈中的 AI 只生成建议文字，不生成商品名、价格、图片、状态或金额；在线能力不可用时使用本地建议模板，不影响订单写入。

### 5.6 消费测试、报告与分享

1. 点击开始测试后播放扭蛋摇动，停止后显示结果，再进入固定布局、可上下滚动的报告。
2. 规则人格仍由 9 类人格加“欲望观察员”确定；版本化映射再选择 Figma 的 20 张完整视觉卡之一。视觉卡不裁切，也不参与评分。
3. 报告保留人格分析、比例、三个固定场景和建议文字。商品区从“最终购买/已剁手”订单中按实际金额降序取最多三件；不足三件时如实少显示，不编造个人订单。
4. 分享层直接展示选中的完整人格视觉卡，下面只保留“返回”和“保存到相册”。
5. 历史列表保存报告快照及其规则版本、数据 revision 和生成时间；点击历史项目重新打开原报告，不以当前订单重新随机生成。

### 5.7 单个目标与四态滑块

- 活动版只有一个当前确认省下目标，快捷额度或手动输入都更新该目标。
- 滑块使用连续数值，视觉和文案按 ¥600、¥3,000、¥7,000、¥10,000 四个 Figma 代表状态切换；使用相邻代表值中点作为状态边界，跨过边界时切换中间 IP 形象、标题和说明。
- 目标进度只由目标创建之后进入“幸好没买/回血中”的订单金额重算，不能使用随机数伪装真实进度。

### 5.8 AI 交互

首次 AI 诊断是主体验，不做开放式聊天首页。病历本先呈现本地事实，再出现“请它看看”。用户能看到将发送的汇总范围，并可不同意；不同意时仍获得本地人格和规则建议。

AI 首次结果固定为：消费人格、两条数据依据、模式解读、一步建议、目标关联和边界说明。之后最多选择 3 个引导式问题、完成 3 轮追问。AI 可以润色报告与下单反馈中的建议文字，但不接管人格、商品、金额、状态、目标或报告商品排名。

P0 保留小浣熊作为主 IP，但不把它做成开放式 AI 对话角色，也不要求口型、自由行走或复杂表情系统。AI 的人格继续通过诊疗终端的命名、文案、诊断标签、状态灯和报告动效表达；小浣熊只补充空间引导和情绪反馈。

### 5.9 区域交互素材需求

| 区域 | 现阶段可直接实现 | 获得分层素材后增强 |
| --- | --- | --- |
| 手机 | 全景热点 + 原创 DOM 手机屏幕 + CSS 浮起 / 转正 | 手机透明切图、桌面遮挡层、贴合轮廓遮罩 |
| 订单小票 | 原创 HTML / SVG 小票、印章和档案盒 | 纸张纹理、桌面前后景遮挡层 |
| 钱包病历本 | 原创 DOM 诊疗界面、扫描线、状态灯和报告卡 | 屏幕独立图层、屏幕反光 / 发光遮罩 |
| 白板 | DOM 便签、拖动坐标和表单；按已校准四角投影到全景白板表面 | 无字白板底图、白板前景边框 / 遮挡层，用于进一步增强前后遮挡 |
| 快递堆 | 每个箱子的独立热点、短文案和轻晃效果 | 箱体 / 箱盖 / 胶带 / 内部商品分层图，或每箱透明遮罩 |

没有分层源文件时，优先实现“镜头聚焦 + DOM 内容物 + 区域独有反馈”；不要用模糊的全屏滤镜伪装物件已经被真正分离。

### 5.10 欲望剥壳机

1. 各式本地商品从舞台底部按抛物线出现，外层覆盖一至两层促销、社交、情绪或身份话术；切割只剥开话术外壳，商品本体保持完整。
2. 第一局先提供不计时奶茶教学；正式局固定 45 秒，依次进入单层、混合和聚焦阶段。漏接不扣分，不提供排行榜、奖励或连续登录机制。
3. 话术来自版本化本地目录并按种子确定性选择；个人冷静单只允许用户已经明确选择的结构化原因参与话术家族匹配，证据不足时使用中性问号外壳。
4. 聚焦阶段可以读取最近一笔非演示“欲望冷却中”订单的名称、品类和已有金额；没有个人订单时回退到本地商品池，且不得为商品池虚构价格。
5. 普通输入统一为切割线段；触屏、鼠标、方向键加 Enter / Space，以及体感食指轨迹都进入同一命中函数。体感识别只在用户主动开启后运行于本地，不上传画面，也不能触发“放进冷静区”等业务动作。
6. 结果页只展示本局实际剥开的原话术和固定的“没什么感觉”，据此生成一条不持久化的轻提醒。“放进冷静区”只打开预填表单，仍需用户确认提交；“只是玩玩”直接返回房间。
7. Canvas 只承担商品、外壳、轨迹与短反馈；标题、规则、键盘控制、当前目标、复盘和业务按钮均使用语义化 DOM。Canvas 不可用时切换为可聚焦的目标列表；减少动态时移除旋转、飞散碎片和高速位移。

## 6. 系统架构

```text
Hash 路由 / 原生 DOM ─────────► 页面 / 内容层
        │                           │
        ▼                           ▼
场景状态适配器 ◄──── 事件桥 ─── app.js 状态层
        │                           │
        ▼                           ├──► personality-scoring.js
球形全景 + 热点注册表               ├──► LocalStorage 版本化存储
                                    ├──► JSON / CSV 本地导出
                                    └──► /api/ai/diagnosis
                                               │
                                       同源 Node 服务
                                               │
                                DeepSeek 适配 + 证据 / Schema 校验
```

### 6.1 当前模块边界

- `app.js`：Hash 路由、订单状态机、本地存储、功能内容层和 AI 前端状态机。
- `personality-scoring.js`：7/30 天五维评分、动机占比、人格匹配、置信度、目标进度和最小化请求；保持纯函数可复算。
- `panorama.js`、`scene-config.js`：场景层、热点注册表、全景白板四角、屏幕 / 球面双向投影、焦点与数据反应，不保存业务事实。
- `peel-game.js`、`peel-copy-catalog.js`：欲望剥壳机纯状态机、抛物线实体、确定性话术和冷静单只读适配，不访问 DOM 或存储。
- `peel-game-ui.js`、`peel-gesture-controls.js`：Canvas / 语义降级控制器与本地手势轨迹映射；只产出游戏命中、暂停和恢复事件。
- `budget-whiteboard.js`：把目标便签的归一化坐标投影到全景白板，处理白板内拖动、键盘移动与镜头同步；只回传展示元数据。
- `server.mjs`：静态资源、同源 API、请求尺寸限制、内存限流、安全响应头和错误映射。
- `server/diagnosis-service.mjs`：DeepSeek 请求适配、证据目录、JSON 解析、字段约束和本地事实回填。

活动版暂不引入前端框架和数据库。后续若订单领域继续增长，再把 `app.js` 中的路由、订单、存储和导出拆分为独立模块；当前先保持可直接部署的低依赖结构。

### 6.2 热点注册表

三个核心功能区与多个探索热点共用一个配置源，供房间热点、顶部三个语义入口和自动化测试使用：

```ts
type HotspotId = 'sofa-phone' | 'gym-screen' | 'whiteboard' | 'desire-peel' | `package-${number}`

type Hotspot = {
  id: HotspotId
  label: string
  panel: 'new' | 'orders' | 'clinic' | 'goals' | null
  yaw: number
  pitch: number
  focus: { yaw: number; pitch: number; fov: number }
  kind: 'feature' | 'activity' | 'thought'
  priority: 'primary' | 'secondary'
  getStatusText(view: ProductView): string
}
```

避免在 SVG 图层、顶部语义入口和热点标签中分别维护链接，导致名称、顺序或状态不一致。

白板使用同一配置中的四个球面角坐标定义物理表面。当前目标便签的 `note.x / note.y` 先在该四边形内插值得到球面位置，再由全景相机投影到 DOM；拖动时反向把指针位置还原为球面坐标并求回归一化坐标。这样唯一当前便签会跟随镜头平移和缩放，而不是固定在浏览器屏幕上。目标面板提供快捷额度、连续四态滑块、手动编辑与保存，不再提供并行生效的目标列表。

界面与业务计算同时只暴露一个当前目标。目标进度按目标创建时间之后进入“幸好没买”的订单重新计算；更新目标额度不制造第二个并行生效目标。为兼容旧数据，当前持久化结构仍保留 `goals[] + activeGoalId`，并允许演示目标与个人目标隔离保存；视图只渲染当前目标。若后续迁移为单一 `currentGoal` 字段，必须保留历史订单且提供版本化迁移测试。

分享层使用 Figma 提供的 20 张完整人格视觉卡之一，不对卡片主体二次裁切；卡片下方只提供“返回”和“保存到相册”。保存图片在浏览器本地完成，不上传订单数组，不包含商品名、备注、订单 ID 或身份信息。

## 7. 本地数据与导出

P0 当前使用一个版本化业务 LocalStorage envelope，并用独立键保存报告历史：

```ts
type Goal = {
  name: string
  amount: number
  createdAt: string
  deadline?: string
  noteText?: string
  note?: {
    x: number        // 白板内归一化中心坐标 0..1
    y: number
    color: 'yellow' | 'cyan' | 'coral' | 'acid'
    rotation: number // -4..4 度
  }
}

type ReportSnapshot = {
  id: string
  generatedAt: string
  period: 7 | 30
  canonicalName: string
  personaName: string
  cardId: string
  orderCount: number
  confidence: number
  dataMode: 'personal' | 'demo'
  source: 'local' | 'hybrid'
  assessment: LocalAssessment
  presentationInference: PersonaInference | null
  productGroups: PurchasedProductSnapshot[]
  adviceSteps: string[]
  goal: GoalProgressSnapshot | null
}

type LocalEnvelope = {
  schemaVersion: number
  dataRevision: number
  orders: Order[]
  goals: Goal[]             // 兼容字段；每个数据范围只保留一个目标
  activeGoalId: string | null
  settings: {
    onboardingSeen: boolean
    aiConsent: boolean
    reducedMotionOverride?: boolean
  }
  latestAiResult?: {
    sourceRevision: number
    period: 7 | 30
    generatedAt: string
    result: ValidatedAiResult
  }
}

type ReportHistoryStore = ReportSnapshot[] // 独立、受数量上限约束的本地键
```

- 任何订单或目标业务内容变化都递增 `dataRevision`；便签坐标、颜色和旋转只属于视图元数据，不使 AI 结论过期。
- `sourceRevision !== dataRevision` 时，AI 结果过期。
- 所有金额、排名和进度均由 `orders` 与 `activeGoalId` 指向的当前目标重新计算。
- 商城“我的商品”不另存业务数组，而是按订单品类从 `orders` 派生；购物车图标也读取同一订单源。
- 独立的报告历史存储保存当次规则版本、数据 revision、视觉卡映射、聚合结果与最多三件已购商品快照，使历史可重开且不因当前订单变化而偷偷重算。若以后并入业务 envelope，需要单独做迁移，不能静默丢弃旧历史。
- 测试完成后的 `skipIntroOnce` 使用当前标签页的一次性会话状态，刷新消费后立即清除；它不属于长期用户设置。
- 演示数据保留 `demo: true`，默认不进入个人导出。
- JSON 完整备份和 CSV 订单表在浏览器本地生成；CSV 防止以 `= + - @` 开头的输入被表格软件执行。
- 存储不可用时保留内存会话并明确提示“刷新后可能丢失”，不能静默假装已保存。

## 8. AI 请求与输出契约

### 8.1 前端发送的最小汇总

```ts
type WalletSummaryRequest = {
  period: 7 | 30
  scoringModelVersion: string
  totals: {
    recorded: number
    saved: number
    purchased: number
    coolingCount: number
  }
  categories: Array<{ category: string; count: number; amount: number }>
  reasons: Array<{ reason: string; count: number }>
  statuses: Array<{ status: string; count: number; amount: number }>
  goal?: { name: string; targetAmount: number; progress: number; progressRate: number }
  dataMode: 'personal' | 'demo'
  localAssessment: {
    axes: Record<'I' | 'P' | 'R' | 'X' | 'E', number | null>
    motives: Record<'function' | 'emotion' | 'social' | 'value', number>
    outcomes: Record<string, number | null>
    persona: { primary: string | null; secondary: string | null; walletTheme: string }
    confidence: { level: string; score: number; effectiveSampleSize: number }
  }
}
```

默认不发送订单 ID、商品名、备注、单笔时间、导出文件、浏览历史和真实身份。前端 API 密钥为零；真实模型请求只通过同源代理发出。服务端依据这份汇总生成有限的 `evidenceCatalog`，模型只能选择其中的证据 ID。

### 8.2 结构化结果

```ts
type ValidatedAiResult = {
  persona: { title: string; secondary: string | null; summary: string }
  evidence: Array<{ id: string; label: string; statement: string }>
  pattern: string
  action: { title: string; steps: string[] }
  goalLink: string
  disclaimer: string
  suggestedQuestions: string[]
  provider: 'deepseek'
  model: string
}
```

服务端限制字段长度和数量，并把规则人格标题强制回填为本地评分结果，把证据 ID 映射为服务端已知事实。Figma 20 张视觉卡由前端依据本地规则人格、钱包主题、规则版本和稳定种子确定，在线能力不参与选卡。模型自由生成的金额、次数、自创人格名、商品名或价格不会进入页面，前端还会用同一套事实规则再次校验。在线能力无权修改五维、动机、规则人格、视觉卡、订单、目标、同意状态或导出文件。

推演期间的四阶段只是展示控制器，不写入业务状态：`本地规则已计算 → 聚合摘要已准备 → 等待真实回复 → 返回结构已校验`。第四阶段只能在响应、Schema 与当前数据上下文都真实通过后出现；慢请求停留在等待阶段，错误直接进入本地降级。减少动态时取消 850ms 阶段停留和约 500ms 报告位移，但不会跳过真实网络等待或校验。

### 8.3 最小后端

阿里云上的同源 Node 服务只负责：静态资源、读取服务端密钥、限流、输入尺寸校验、DeepSeek Chat Completions JSON Output 适配、12 秒内超时、输出 Schema 校验和必要的错误码映射。默认模型是可通过环境变量替换的 `deepseek-v4-flash`。它不创建用户账户、不保存历史会话、不记录完整输入输出；Caddy 负责 HTTPS 和域名反向代理。

## 9. 响应式、可访问性与减少动态

| 宽度 / 模式 | 房间 | 内容层 | 导航 |
| --- | --- | --- | --- |
| 375px | 可拖动全景、简化热点标签 | 全屏页面；仿真手机去除外围背景与设备壳 | 顶部三个紧凑语义入口 + 房间热点 |
| 768px | 可拖动全景、保留热点光效 | 全屏或大抽屉 | 顶部三个语义入口 + 房间热点 |
| 1440px | 完整房间切面、轻视差和推近 | 右侧面板或全屏内容层；开始买吧保留背景承托的仿真手机 | 顶部三个语义入口 + 房间热点 |
| 减少动态 | 无视差、无位移推近、无循环动画 | 直接切换 | 所有功能不受影响 |

- 每个热点必须存在语义化按钮或链接、可见文字、清晰焦点和至少 44×44px 的触控面积。
- 内容面板打开后焦点进入标题，关闭后回到触发物件。
- AI 加载、成功、错误和降级使用克制的状态播报。
- 颜色不是唯一状态线索；“欲望冷却中、回血中、已剁手”均有文字与图标，底层状态仍可纠正。
- 房间不是唯一导航。插画、SVG 或动画加载失败后主流程仍可完成。

## 10. 性能与韧性预算

- 普通访问先播放约 3 秒一次性开场，再进入 Figma 节点 `4:5` 的全屏静音循环等待；只有等待阶段显示唯一“开始”入口。
- 开场片段、循环片段和静态封面必须使用一致的视口裁切与定位；视频失败时静态封面继续提供开始入口。
- 页面转场 300–500ms，状态数字反馈不超过 600ms。
- 除已确认的两段开屏视频外，P0 不加载大体积 GLB、额外全屏视频、远程字体或持续音频；视频应提供与显示尺寸匹配的清晰导出并控制码率，全景纹理按设备 `MAX_TEXTURE_SIZE` 自动降采样。
- 循环动画在页面隐藏、场景离开视口和减少动态模式下停止。
- 单个场景资产 404 时显示顶部语义入口或等价文字降级入口，不白屏。
- AI 不可用时，本地订单、统计、目标、导出和规则建议全部可用。

## 11. 验证矩阵

| 场景 | 视口 | 输入 | 数据 | 网络 | 预期证据 |
| --- | --- | --- | --- | --- | --- |
| 首次进入房间 | 375 | 触控 | 空 | 在线 | 约 3 秒一次性开场、无缝切换节点 `4:5` 循环等待、唯一开始按钮、点击后进入房间的证据 |
| 电脑 → 下单 → 确认省下 | 1440 | 键盘 | 个人 | 在线 | 完整录屏/步骤截图、数字重算 |
| 房间入口、热点与后退 | 375/1440 | 触控/键盘 | 演示 | 在线 | 无可见底部导航；顶部三个入口、热点、焦点顺序、URL、Esc、后退均通过 |
| 开始买吧与订单同源 | 375/1440 | 触控/键盘 | 演示 | 离线 | PC 有仿真手机承托、移动端无外围框；手动商品、我的商品、订单与购物车图标同步 |
| 消费报告与历史 | 375/1440 | 触控/键盘 | 个人/演示 | 在线/降级 | 9+观察员规则映射视觉卡、最贵三件已购、历史重开、分享保存 |
| AI 首次同意与成功 | 1440 | 指针 | 演示 | 在线 | 发送摘要、请求字段断言、结果截图 |
| AI 超时与无效结构 | 375 | 触控 | 个人 | 故障注入 | 明确降级、本地功能继续可用 |
| 刷新与直接 URL | 768 | 键盘 | 个人 | 在线 | 普通深链可恢复；测试完成后的紧接刷新跳过两段开屏并回房间，标记随后清除 |
| JSON / CSV 导出 | 375/1440 | 触控/键盘 | 含特殊字符 | 离线 | 文件可解析、无上传请求、CSV 安全 |
| 减少动态 | 768 | 键盘 | 个人 | 在线 | 无视差/位移，主流程通过 |
| 欲望剥壳机 | 375/1440 | 触控/鼠标/键盘 | 空/个人 | 离线 | 商品从底部抛起、外壳逐层剥开、本体不碎、个人金额不伪造、结果可选没感觉、关闭焦点归位 |
| 欲望剥壳机体感 | 1440 | 摄像头食指轨迹 | 个人 | 离线 | 主动授权、本地识别、丢手暂停、普通输入可随时回退、结算或主动停止后摄像头关闭 |
| 场景资产失败 | 375 | 触控 | 空 | 资产 404 | 顶部语义入口或等价文字降级入口出现、无白屏 |

文档完成、代码实现、自动化通过、浏览器验证、部署、真实模型服务验证必须分别记录。

## 12. 里程碑

1. **M0 设计基线**：冻结本文件、热点地图、AI Schema 和资产边界。
2. **M1 空间壳竖切**：房间、诊疗终端、四个热点、URL、聚焦/返回、顶部三个语义入口和减少动态。
3. **M2 本地业务闭环**：下单、订单状态、统计、目标、本地存储和导出。
4. **M3 钱包病历本**：本地人格、AI 同意、成功、过期、错误和降级。
5. **M4 区域专属交互与数据反应**：补手机浮起、票据归档、病历扫描、白板便签、快递探索，以及场景物件随订单和目标变化。
6. **M5 浏览器证据与部署**：375/768/1440、键盘、移动端、故障注入、生产构建和演示彩排。

## 13. 已登记偏差与暂不决定项

| 项目 | 参考机制 | 项目决定 | 原因 | 重新考虑条件 |
| --- | --- | --- | --- | --- |
| 空间移动 | 参考项目允许自由行走、碰撞和相机跟随 | P0 允许拖拽环视和镜头聚焦，不允许角色自由行走 | 首版核心是消费理解，不是移动玩法 | 用户测试证明自由探索本身显著提升完成率 |
| 渲染技术 | 参考项目使用复杂 WebGPU / Three 场景 | P0 使用原创轻量 WebGL 球形全景渲染 | 满足全景目标，同时避免复制引擎与引入建模成本 | 只有遮挡、独立物体动画成为核心需求时升级完整 3D |
| 多人 | 参考项目含实时玩家 | P0 无账户、无多人 | 与本地私密消费记录冲突且无核心价值 | 未来出现明确的匿名共同挑战需求 |
| AI 角色 | 参考体验可用角色承担人格与反馈 | 小浣熊作为主 IP 负责入口与引导；诊疗终端独立承担全部 AI 职责 | 保留统一世界观，同时避免角色遮挡数据或垄断任务入口 | 若后续增加 AI 对话，需重新验证隐私、状态与可访问性边界 |

## 14. 待在低保真原型中验证的问题

1. 健身区竖屏能否在 10 秒内被理解为消费测试主入口，而不是健身装饰。
2. 当前“桌面右侧面板、移动全屏层”的组合是否符合首轮用户理解。
3. 钱包病历本屏幕能否在不依赖小浣熊文案的情况下清楚表达 AI 的空闲、思考、成功、过期和降级状态。
4. 订单角标、目标进度和热点状态中，哪些能帮助理解，哪些只是噪声。
5. 获得分层源文件后，应优先为哪几个物件制作贴合轮廓的发光遮罩。

这些问题不阻塞当前全景场景竖切；主 IP 开屏先完成，小浣熊的复杂动画、真实 AI 和物件轮廓遮罩分别进入后续里程碑。
