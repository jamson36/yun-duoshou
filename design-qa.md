# “让你花个爽！”Figma 全面对齐 Design QA

## 当前结论

- 当前版本按 Figma 的桌面端与手机端画板分别实现：桌面业务页面使用可用视口全屏展示，手机端使用独立的移动画板与底部导航；不再采用“左侧放大场景、右侧放业务面板”的旧布局。
- 本文记录的是本地实现和本地验收边界，不代表已经提交、推送、上传或部署。生产地址是否更新必须以单独的发布记录和生产回归为准。
- Figma Design 文件用于核对页面尺寸、元素、图片和静态状态；Figma Make 文件只采用当前工具能够读取的源码、资源和时序证据。Make 文件不支持原生 motion context，因此不得据此宣称其 17 张图片已经逐张核验，也不得宣称所有原型轨道或原生动效已经验证。

## 对照来源与验收原则

- 设计来源：Figma `让你花个爽！` Design 文件。商城首页、模拟商品列表和详情等已按对应桌面 / 手机 Frame 的画板比例、内容层级和状态进行映射；曾使用的 Frame `5:25` 只作为商城相关页面的其中一个对照节点，不再代表整个产品的唯一画板。
- 动效来源：Figma Make `需求界面设计` 中可读取的实现结构、资源和时序。无法读取的原生动效元数据不作为“已验证”证据。
- 产品边界优先：页面保留“模拟下单、不会付款或发货、确认省下只是产品内记录”等事实，不为了逐字复制设计而暗示真实支付、真实存款或心理诊断。
- 视觉验收必须同时覆盖桌面与手机、选中与未选中、打开与返回、动效与减少动态效果，不能用单张截图代替交互验证。

## 页面与元素覆盖

### 1. 三秒房间开屏与主界面

- 开屏固定约 3 秒且不可跳过；网页实际播放由设计视觉包转码得到的 1920×1080 H.264 视频。视频开始播放后停用 Canvas 重复绘制；自动播放失败、媒体错误或停滞时才回退到像素揭幕，且两条路径都不会缩短三秒门禁。
- 三秒揭幕视频只播放一次；门禁结束后切换为本地 `room-entry-loop.mp4` 并循环播放，点击开始后停止。其构图对齐 Figma `4:5`，但该节点没有时间轴、内嵌视频或 GIF，不能把本地视频描述为 Figma 原生导出；视频失败或减少动态效果时回退到同构静态入口。
- 房间三个核心入口分别使用 Figma `32:3637`、`32:3643`、`32:3644` 导出的 190×69、190×79、170×71 PNG。资源加载后替换旧圆点卡片，加载失败时保留语义按钮和旧视觉回退；桌面原始画板下不再被旧 56px 规则压缩。
- 全景资源加载与三秒计时并行，资源失败时仍在计时结束后提供静态房间和文字导航，不因装饰资产失败白屏。
- 点击进入后的镜头过渡保持在约 300–500ms，并支持减少动态效果。
- 桌面房间和业务页面均占满可用视口；手机端不缩放桌面画板，而是使用独立布局、文字入口和底部导航。

### 2. 假装下单首页与模拟商城

- 首页保留 Figma 的暖白底、深蓝数据主卡、黄色 / 橙色强调、大圆角卡片和小浣熊 IP。
- “我的消费曲线”同时绘制橙色平滑趋势线、面积、峰值和七日柱形标签；“当前只有柱图”已不是现状。
- 分类入口包含对应分类图标、选中 / 未选中状态和明确文案；“分类只有文字色块”已不是现状。
- 点击“开始买吧”先进入 Figma `5:25` 对齐的介绍页；桌面端点击圆形箭头后进入 `393×844` 模拟手机，手机端则直接使用全屏业务画板、不重复套手机外壳。关键操作目标不小于 44×44px。
- 商城支持 `#new` 首页、`#new?view=catalog&type=<type>` 列表和 `#new?view=detail&type=<type>&product=<id>` 商品详情。刷新详情页保留当前状态；直接打开的深链返回到商城列表，不把用户送出站点。
- 桌面商品详情采用图片与文案 / 行动区并列的画板结构，使价格、标签、统计和主操作在首屏内形成完整层级；手机详情为自然纵向流，主操作不再悬浮遮挡统计或底部说明。

### 3. 回血计划三页

- “消费小目标”“欲望控制器”“剁手清单”使用同一套顶部标签、背景、内容画板和约 320ms 页面进入动效。
- 从任意一页切换到另外两页均触发同一过渡，不再只有点击“剁手清单”时出现弹出动画。
- 减少动态效果开启时移除大幅位移和循环动效，保留即时状态切换与可见焦点。
- 订单状态、目标进度和确认省下仍从源记录重算；“冷静中”与“最终购买”不会进入确认省下和目标进度。
- 消费小目标的十颗珠子均可点击，每颗对应 ¥1,000，移动端实际点击尺寸为 56×56px；目标金额上限为 ¥10,000，¥10,001 会在表单内直接提示并阻止保存。历史已存在的超限金额只允许原值展示，不会静默截断或继续改成另一笔超限值。
- 欲望控制器在 ¥600、¥3,000、¥7,000、¥10,000 四个节点分别切换从容观察、需要留白、建议缓冲、先停一晚的角色图和钱包提示，全部使用本地状态，不修改订单或目标事实。

### 4. 钱包病历本、人格扭蛋与结果

- 扭蛋机使用独立运动控制器：待机时六颗球在透明舱内低速碰撞、旋转和反弹，抽取时提高驱动力与速度上限并让整机进入摇动，揭晓后自然减速；不再只是固定位置上的 CSS 上下跳动。
- “只看本地人格”同样先完成真实摇球再揭晓，常规动效约 1.4 秒，减少动态效果时即时显示静态结果。该路径不显示数据确认弹窗、不发送聚合摘要；关闭结果后焦点返回本地抽取按钮，结果卡上的“再测一次”继续走本地路径。
- 扭蛋状态覆盖可抽取、确认、旋转、揭晓、本地降级、过期和证据不足；旋转过程中仍显示真实的分析阶段和撤回入口。
- 在线解释只补充证据选择、解释与建议；本地版本化规则仍是维度分数、主人格、置信度和金额次数的事实来源。在线响应通过结构校验后才进入“报告生成完成”，失败时明确回到本地结果。
- 人格揭晓弹层与报告面板只打开一次，关闭、重新抽取和打开报告的焦点路径保持一致，避免重复闪烁。

### 5. 消费人格海报

- 分享海报的内容画布保持 `1080×1538` 比例，并映射为 Figma 的两套展示尺寸：桌面卡片 `380×611`，手机卡片 `244×392`。
- 桌面版由约 `380×541` 海报主体和 `70px` 主操作组成；手机版由约 `244×347` 海报主体和 `45px` 主操作组成，实际按钮仍保持至少 44px 可点击高度。
- 海报包含趣味人格名、标签、规则底座、近期证据和低压力建议；Figma 趣味人格只负责展示映射，不覆盖本地规则输出，也不会编造订单、金额或身份属性。

## 2026-08-31 指定节点对齐复核

- 主页以 Figma `32:3631` 的房间导出作为 `assets/room-panorama.webp` 的视觉基准，仍使用球形全景而不是把静态截图铺成不可交互背景；桌面初始视角聚焦沙发主入口，左右拖动可查看健身区和白板。问候气泡与小浣熊使用同一个全景投影点，不再漂在固定屏幕角落。
- 三个入口继续使用 Figma 4x 热点素材；“开始买吧”的图标层有独立居中校正，气泡、图标、文字和数量徽标保持各自层级。
- 点击“消费测试”直接进入 Figma `20:2` 起始画面，不先展示报告或旧病历头部。扭蛋机改用透明背景正式导出 `assets/personality-gachapon-figma.png`，桌面与 `390×844` 手机端均保持 contain、无横向溢出。
- 生成结果使用 Figma `60:544` 的金色胶囊素材 `assets/figma-gachapon-result.png`，结果卡收敛为“再测一次 / 打开报告”两个操作；人格名称仍由本地规则结果决定，不被静态设计图覆盖。
- “打开报告”切换到独立纵向滚动视图。商品卡只从 `status = purchased` 的最终购买记录聚合，按同名商品合计金额稳定取最贵三项；不足三项时不伪造占位商品。演示数据已补齐三条最终购买记录用于完整 Demo。
- 分享弹层直接展示 20 套人格注册表中的对应 4x 插图，海报画布为 `1080×1538`，图片使用 contain 不裁切；弹层可见操作仅保留“返回”和“保存到相册”。
- 本轮把主页、测试起始页、结果卡、报告首屏和分享弹层分别与已保存的 Figma 截图放在同一张左右对比图中复核。测试起始页、结果卡、报告层级和分享按钮已收敛；主页与 Figma 全景展开图的构图差异来自“可旋转球形视口”机制，源房间图与热点仍来自同一节点，不以牺牲旋转能力换成静态全景图。

## 交互与可访问性验收

- Hash 路由是页面状态的唯一来源；房间热点、文字导航、底部导航、浏览器后退和 `Esc` 使用同一套页面状态，不再因重复改写 Hash 造成双闪。
- 同一时间只激活一个业务区域。关闭后镜头归位，焦点返回触发入口。
- 关键按钮、标签页和返回入口支持键盘操作、可见焦点和辅助技术状态；手机端点击区域至少 44×44px。
- 页面切换、商城详情、回血三页、扭蛋揭晓和海报弹层均需检查无横向溢出、遮挡、双闪或阻断性控制台错误。
- 减少动态效果必须覆盖开屏后的镜头过渡、回血页面切换、趋势变化、扭蛋运动、结果揭晓和弹层进入；静态状态与业务功能仍然可用。

## 2026-09-01 房间、扭蛋、返回与目标表单复核

- 主房间改用 `assets/room-panorama-hd.webp`（8192×4096）作为球形全景纹理。该原图直接包含坐在红色圆凳上的完整小浣熊，不再叠加站立角色图；桌面默认视角和手机默认视角分别校准，仍可左右拖动旋转。浏览器实测拖动后角色投影锚点移动约 158px，证明当前不是静态背景替换。
- 扭蛋机不再依赖整机 PNG 中烘焙的静态球。六颗 512×512 透明 PNG 独立挂入碰撞控制器；待机 0.9 秒内可见位移约 1.68–8.14px，点击“开始测试”后的 320ms 位移约 21.25–69.37px，并继续服从页面隐藏、减少动态效果和销毁状态。全新无记录用户处于 `locked` 资格状态时也保留克制的环境运动，实测六球 0.9 秒位移约 1.96–11.07px，不再出现首次进入完全静止。
- `#panelClose`、`#commerceBackButton` 与 `#clinicReportBackButton` 统一使用 `assets/figma-chevron-left.svg`、20×20px 箭头、暖白半透明圆底、同一边框 / 阴影 / 焦点反馈；桌面导航按钮为 46×46px，手机为 44×44px。手机消费测试、消费小目标和开始买吧返回入口均固定在左上安全区；商城子页保持在同一左侧标题栏列。
- “消费小目标”两组双列控件统一为 48px 高并从同一顶边开始。浏览器实测桌面 1440px、手机 390px 与 375px 下，目标金额 / 当前已存、预计完成时间 / 每周建议减少支出两组顶边误差均为 0，宽度误差不超过 1px，且无横向溢出。
- 已把 Figma / 保存参考与本地实现放入同一张对比图复核：`output/playwright/compare-room-desktop.png`、`compare-gachapon-desktop.png`、`compare-goals-desktop.png`、`compare-goals-mobile.png`。房间右侧实现图是同一全景素材在球形相机中的视口裁切，属于保留可旋转机制后的预期差异；目标页的演示数据和多目标信息属于现有业务内容，不作为视觉错位处理。
- Figma Design 权限已于 2026-09-01 恢复，`5:25`、`30:977`、`30:1334`、`30:1936` 与 `4:5` 已重新读取并完成高倍导出。六颗独立扭蛋仍是从已保存视觉派生清理的透明素材，不描述成 Figma 原始导出。

## 本地验证矩阵与证据边界

- 布局基线：本轮实际覆盖 Figma 原始桌面 `1920×1080` 的左右逐屏对比、桌面 `1440×1000` 与手机 `390×844`；历史矩阵另含平板 `768×1024` 和 `375px` 手机宽度。布局验收以 CSS 视口为准，不把桌面缩放截图当作手机端证据。
- 核心路径：三秒开屏 → 房间 → 假装下单首页 → 分类 / 商城列表 → 商品详情 → 返回；消费小目标 ↔ 欲望控制器 ↔ 剁手清单；钱包病历本 → 扭蛋旋转 → 人格揭晓 → 报告 → 双尺寸海报。
- 状态路径：新建订单 → 冷静中 → 幸好没买 / 最终购买 → 趋势、人格和目标重算；不足 3 笔、初步人格、充分证据、数据变化后过期、在线失败降级和减少动态效果。
- 自动化证据覆盖开屏固定时长与视频回退、Figma 开屏/热点素材、商城深链、回血三页一致动效、预算一万元边界、四档角色图、商城详情布局、海报双尺寸、人格映射、扭蛋待机/抽取碰撞、本地揭晓、在线解释结构校验和请求取消。本轮完整测试为 `231/231` 通过；测试通过只能证明相应合同，不等于真实手机、生产环境或现场用户验收。
- 本轮浏览器实测确认：视频在约 0.9 秒时处于真实播放状态、3 秒后才开放按钮；1920×1080 入口卡与房间主画板已同 Figma 截图并排检查；桌面和手机均无横向溢出；人格本地抽取先旋转后只打开一次结果；减少动态时球体静止且即时揭晓；预算 ¥10,001 被拒绝、十颗珠子和四档角色切换有效。
- 浏览器验收必须使用本轮当前静态资源版本，检查缓存参数，避免旧 JS / CSS 让页面看似仍是上一版；本地截图或浏览器结果不得外推为生产已经更新。

## 已知边界与后续门禁

- Figma Make 的原生 motion context 当前不可读取；现有动效来自可读源码、资源、时序和浏览器交互对照。这一限制不影响本地实现，但禁止写成“Make 所有动图和原型均逐一验证”。
- Figma Design 权限已恢复，当前商城和开屏所需静态素材已经导出。`4:5` 的动效清单为空，节点内没有可导出的原生视频 / GIF；整帧 SVG 扫描超过接口 20 项上限，但实现所需主视觉、分页圆点和按钮已按具体子节点单独导出且未截断。Make 文件及未提供时间轴的节点仍不能据静态画面推断动效。
- 设计或 Make 中的图片只有在来源和使用授权明确时才能进入公开版本；当前项目资产与新导出资产仍需在发布前复核授权、压缩、尺寸和降级策略。
- 当前 PRD 使用“一个当前目标”的产品语义，源码则允许保存多个目标并切换当前目标，两者存在实质口径冲突。本轮不静默删除多目标数据、不改变目标进度含义，也不把现状反写进 PRD；需由用户明确选择“收敛为单一当前目标”或“确认多目标并更新产品口径”后，再同步实现、数据迁移和验收标准。
- 当前状态为本地修改与本地验收阶段，未部署。只有用户在看到并确认本轮本地版本后，才能另行执行上传、部署和生产回归。

## 2026-09-02 欲望剥壳机书桌入口复核

- 用户标注参考图与本地 `1440×1000` 实现已放入同一张对比图复核：`output/design-qa/peel-hotspot-compare.jpg`。参考图红框指向书桌上的实体掌机；实现图的“小游戏 / 欲望剥壳机”标签和四角定位反馈已锚定同一物件。
- 热点以高清全景原图 `8192×4096` 中约 `(6124, 2226)` 为锚点，换算为 `yaw = -89.12°`、`pitch = -7.82°`；聚焦镜头沿用该中心，避免标签随镜头旋转漂回左侧陈列架。
- 房间层不再用 CSS 重绘橙色掌机，只在原图中的真实 Switch 上叠加克制的深色玻璃标签与角标；文案同步收敛为“书桌 · 掌机”和“剥开催你下单的信号，看看商品本身”。
- 本地浏览器确认鼠标点击和键盘 `Enter` 均可打开小游戏，`Esc` 可关闭并把焦点返回入口；控制台无 warning / error。当前证据仅代表本地浏览器，不代表真实设备或生产环境验收。

final result: passed
