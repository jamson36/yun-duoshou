# 让你花个爽！交互借鉴与建设计划

> 模式：交互机制借鉴，不做 1:1 复刻
> 产品决策源：`DESIGN.md` 与 PRD
> 本文件用途：前向任务、偏差、风险和验证证据账本

## 0. 执行规则

1. 参考站只用于研究机制，不复制无许可代码、素材、文案或品牌。
2. 参考事实、产品决定、实现状态和浏览器证据分别记录。
3. 先完成一条端到端竖切，再扩展场景和装饰。
4. 3D / 2.5D 场景不是唯一导航；桌面和移动端均保留顶部三个可键盘操作的语义入口，且减少动态模式必须同等可用。可见底部导航已被最新确认替代。
5. AI 只解释本地确定性统计，不掌握业务写权限。
6. 每个里程碑同时更新本计划的结果、证据和下一个入口。

## 1. 已提炼的参考机制

| 机制 | 参考中的作用 | 项目转译 | 决定 |
| --- | --- | --- | --- |
| 场景状态机 | 控制相机、雾、区域和交互开关 | 控制房间 idle/focus/panel/reduced/suspended | 采用 |
| 路由 ↔ 场景同步 | URL 切换对应 3D 状态 | 静态原型以 `#new`、`#orders`、`#clinic`、`#goals` 投影到对应焦点，正式路由再映射语义路径 | 采用 |
| 热点注册系统 | 按距离/点击/键盘触发区域 | 统一配置三个核心物件、快递探索组、顶部三个语义入口和可访问名称 | 采用但简化 |
| 事件桥 | 连接 3D 引擎与页面层 | 连接订单/目标事件与场景反馈 | 采用 |
| 本地任务状态 | 记住教学与进度 | 版本化保存订单、目标、同意和结果 | 采用 |
| 三层工程证据 | 镜像、可追溯移植、可读源码 | 参考笔记、原创实现、浏览器证据 | 采用原则，不复制代码 |
| 功能终端作为空间交互者 | NPC 提示、任务和反馈 | 钱包病历本诊疗终端承担引导、解释和追问 | 采用职责，不依赖角色形象 |
| 双阶段开屏 | 用一次性开场建立节奏，再以循环等待承接用户操作 | 约 3 秒全屏一次性开场后无缝进入 Figma 节点 `4:5` 的全屏循环画面；等待页只放一个开始按钮 | 已确认目标，待浏览器验收清晰度与无缝衔接 |
| 主 IP 入口定格 | 用角色与标题建立进入世界的仪式感 | 小浣熊保持同一角色体系；角色、标题和背景由两段授权视频承载，不再叠加三个小入口 | 采用，角色不承担 AI 判定 |
| 可检查物件 | 物件被选中后浮向镜头、可操作并归位 | 手机浮起、报告吐出、白板转正、快递开箱；退出时恢复原场景 | 采用机制，动作与素材原创 |
| 固定相机的 3D 展陈 | 在独立场景中以射线 / 直接操控检查物体 | 掌机进入欲望观察舱，程序化商品可拖动旋转，业务信息保持 DOM | 采用机制，不复制模型、材质或场景源码 |
| 环境随状态变化 | 区域、装置、灯光和角色响应当前状态 | 订单、待复诊和目标进度改变房间物件 | 采用并绑定真实数据 |
| 自由移动与碰撞 | 探索城市 | P0 不采用；P1 只评估固定范围移动和靠近触发 | 分阶段验证 |
| 多人、音频、复杂后处理 | 增强世界感 | 首版没有明确业务价值 | 不采用 |

## 2. 架构假设与验证动作

| 假设 | 为什么重要 | 验证动作 | 状态 |
| --- | --- | --- | --- |
| 球形全景足以建立“进入房间”感 | 决定是否需要完整 3D | M1 做 375/768/1440 三档浏览器验证，再做 5 人快速识别测试 | 浏览器通过，用户测试待做 |
| 三个核心物件无需自由移动也能被发现 | 决定交互复杂度 | 首次进入 10 秒内说出消费测试、手机和白板三个入口 | 浏览器可达，用户测试待做 |
| 中央诊疗区能自然成为视觉主角 | 决定 AI 核心是否成立 | 无提示点击首选与口述理解 | 待验证 |
| LocalStorage 足以承载活动版 | 决定是否需要 IndexedDB | 生成 1,000 笔演示订单，验证读写、迁移和导出 | 基础闭环通过，压力与迁移待做 |
| 结构化 AI + 本地降级能稳定演示 | 决定模型与代理契约 | 成功、12 秒超时、429、无效 JSON 四类故障注入 | 待验证 |
| 不限时的“看商品—关信号—再决定”比抛接玩法更易理解 | 决定活动层是否保留 | 自动化验证状态与数据边界，再由用户在本地直接判断含义与精致度 | 本地实现与浏览器通过，待用户体验验收 |

## 3. 里程碑队列

| 里程碑 | 范围 | 验收门 | 状态 |
| --- | --- | --- | --- |
| M0 | PRD、DESIGN、复刻边界、Skill | 关键决定、模板和风险落盘；Skill 校验通过 | 已完成 |
| M1 | 前端基础、球形全景、诊疗终端、配置化热点、哈希路由 | 375/768/1440 可聚焦三个核心物件和快递区；环视、缩放、返回/后退/刷新通过 | 已完成（本地） |
| M1.5 | 主 IP 强制开屏、像素瀑布、入口定格、镜头拉远和高清全景 | 动画中无按钮且不可跳过；定格与主房间使用同一全景；桌面/移动/减少动态/资源失败通过 | 已完成（本地，待用户验收） |
| M2 | 下单、订单状态、统计、目标、本地存储 | 离线完成非 AI 主流程；刷新保留；数字可复算 | 已完成（本地） |
| M3 | 钱包病历本、同意、AI 代理、Schema、降级 | 成功/超时/429/无效结构均有独立可验证状态 | 进行中（本地交互原型） |
| M4 | JSON/CSV、分享海报、演示与重置 | 导出无上传、CSV 安全、演示数据隔离 | 进行中（JSON、演示数据与本地 PNG 海报已完成） |
| M5 | 区域专属物件交互、场景数据反馈、诊疗终端、响应式和可访问性 | 五类标志性交互、键盘、减少动态、焦点恢复、资产失败回退通过 | 进行中（基础热点与全景白板便签竖切已完成） |
| M5.1 | 2026-09-01 Figma 交互合同对齐 | 顶部入口、双阶段开屏、订单同源、报告历史、单目标与四态滑块在桌面/移动端闭环 | **本地代码、自动化和浏览器通过；用户已确认，进入提交与生产发布** |
| M5.2 | 欲望观察舱 3D 交互 | 真实掌机进入、商品直接操控、短信号关闭、表单交接、WebGL / 减少动态降级和焦点恢复通过 | 本地代码、`394/394` 自动化与四档浏览器通过，待用户验收 |
| M6 | 性能、浏览器矩阵、部署和演示彩排 | 证据矩阵齐；文档/代码/浏览器/部署/真实 AI 分层报告 | 待开始 |

M1 的入口数量以最新场景图为准：健身屏对应消费人格测试，沙发茶几手机同时承载“开始买吧”和订单入口，白板对应回血计划，快递堆承载探索型内心 OS；健身屏内的诊疗终端承担 AI 状态反馈。进入房间后保留三个核心热点，同时以顶部三个语义按钮提供同等入口，不再显示可见底部导航。小浣熊负责双阶段开屏与房间引导，但不取代诊疗终端的 AI 职责。

### 3.1 2026-09-01 已确认的 Figma 对齐任务

本节记录用户已批准的目标状态；所有条目仍需分别核对代码、自动化、桌面/移动浏览器和生产环境，不能因为写入计划就标记完成。

| 范围 | 已确认决定 | 最小验收证据 | 当前状态 |
| --- | --- | --- | --- |
| 房间入口 | 桌面与移动端移除可见底部导航，顶部保留三个语义功能入口，房间热点保留；帮助和 Demo 导入在房间内弱化 | 375/390/768/1440 无横向溢出，三个按钮可触控/键盘使用，热点与按钮映射一致 | 本地代码、自动化和浏览器通过 |
| 双阶段开屏 | 普通访问先播放约 3 秒一次性开场，再进入 Figma 节点 `4:5` 的全屏循环等待；等待页只有一个开始按钮 | 两段画面清晰、铺满、裁切一致、无分层跳位；视频失败静态兜底；循环页无三个小入口 | 本地无叠层与全屏回归通过；动态源最高为 1920×1080，待用户验收清晰度 |
| 开始买吧 | PC 用背景承托仿真手机，移动端直接展示同一手机内容；购物车图标进入订单，不建独立购物车 | 三类商城、详情、模拟下单和订单连续可达；PC/移动布局符合 Figma | 本地代码、自动化和浏览器通过 |
| 订单标签 | 底层状态不变，界面映射为欲望冷却中/回血中/已剁手，已剁手仍可纠正 | 状态切换后标签、统计、目标、报告同步重算 | 本地代码、自动化和浏览器通过 |
| 商品与 AI | 商品名、价格、图片来自用户输入或版本化本地池；AI 只写建议文字 | 相同种子结果一致；AI 成功/失败都不改变商品和订单事实；Demo 有标记 | 本地代码与自动化通过，离线浏览器闭环通过 |
| 人格与报告 | 9 类规则人格加欲望观察员映射 Figma 20 张视觉卡；报告取金额最高的最多三件已购订单 | 规则结果不被视觉卡覆盖；不足三件如实显示；分享卡完整不裁切 | 本地代码、自动化和浏览器通过 |
| 历史与刷新 | 历史可重开当次报告；测试完成后的紧接刷新跳过两段开屏并回房间，普通访问仍走开屏 | 历史内容不被当前订单偷换；一次性刷新标记消费后清除 | 本地代码、自动化和浏览器通过 |
| 当前目标 | 同时只有一个确认省下目标；连续滑块按四个 Figma 代表值切换 IP/文案 | 快捷额度、手动值、四态边界和订单驱动进度均可复算 | 本地代码、自动化和桌面/移动浏览器通过，用户已确认 |
| 同源联动 | 保存手动商品即创建欲望冷却中订单；我的商品、订单、统计、报告和目标由订单源联动 | 新增、改状态、删除后所有消费者一致，无第二套购物车或商品事实 | 本地代码、自动化和浏览器通过 |

返回层级同步纳入上述验收：房间 → 功能首页 → 目录/详情或结果 → 报告 → 分享；页面返回、浏览器后退、移动设备返回和 `Esc` 使用同一层级，弹窗关闭恢复焦点。

本地回归证据（2026-09-01）：完整自动化 `249/249` 通过；浏览器覆盖 375×812、390×844、768×900 与 1440×1000。四态控制器的 ¥600 / ¥3,000 / ¥7,000 / ¥10,000 内容与图片均完成桌面和手机回归，边界 1800/1900、5000/5100、8500/8600 与键盘步进正确；开屏双层同帧为 0，功能页退出后的首次点击可立即响应，页面无横向溢出，控制台 0 错误、0 警告。上述证据不等于真实手机硬件或生产环境验收。

### 3.2 M5 区域专属交互队列

| 优先级 | 交互竖切 | 验收门 | 素材依赖 |
| --- | --- | --- | --- |
| 1 | 手机浮起 → 假装下单 → 小票飞入订单箱 → 打开订单 | 一条链路可连续完成，刷新后数据仍在，减少动态模式可用 | DOM / CSS 可先完成；手机透明切图为增强项 |
| 2 | 小票抬起 → 盖结果章 → 归入对应档案盒 | 两个订单结果视觉和文字均不混淆，失败不播放成功动作 | 原创 HTML / SVG 小票与印章 |
| 3 | 病历扫描 → 指标点亮 → 诊断报告出现 | 本地统计先于 AI 结果；成功、过期、失败、降级可区分 | DOM 诊疗屏；独立屏幕遮罩为增强项 |
| 4 | 白板转正 → 新增便签 → 拖动 / 编辑 → 固定 | 便签坐标可保存，键盘可编辑，移动端有非拖拽替代 | DOM 便签；无字白板与遮挡层为增强项 |
| 5 | 快递轻晃 → 开箱 → 内心 OS → 归位 | 一次只突出一箱，键盘和触控可触发，不造成眩晕 | 基础热点可做；真实开箱需箱体 / 箱盖分层 |

## 4. 有意偏差登记

| ID | 参考行为 | 项目行为 | 原因 | 重新考虑条件 |
| --- | --- | --- | --- | --- |
| D1 | 自由行走城市 | P0 可拖动环视与点击热点；P1 可评估固定范围移动 | 降低首版控制和性能成本 | 功能发现与性能验证均通过，且探索被证明有核心价值 |
| D2 | 完整 WebGPU/Three 渲染 | P0 原生轻量 WebGL 球形全景 + DOM 内容层 | 保留全景效果，同时控制依赖、移动端和可访问性成本 | 独立物体遮挡和动画成为核心需求 |
| D3 | 首次教学控制移动 | 首次引导解释三个核心物件与快递探索区 | 用户无需学习游戏操作 | 引导识别测试失败 |
| D4 | 多人在线与派对彩蛋 | 无社交、无账户 | 消费数据默认私密 | 有明确且安全的社交需求 |
| D5 | 参考体验以角色 / 宠物增强世界感 | 小浣熊确认为主 IP，开屏与房间引导保留；诊疗终端承担 AI 人格与状态 | 统一入口世界观，同时保持 AI 数据界面独立可用 | 若角色升级为可对话 AI，需单独定义隐私、状态与失败降级 |
| D6 | Basement 物件使用真实 3D 网格浮出、旋转和回弹 | 当前用全景热点 + 原创 DOM / SVG 图层模拟检查动作 | 原始场景是扁平全景，表单还需要可访问 DOM | 获得独立物件模型 / 分层素材且性能验证通过 |
| D7 | 参考项目使用完整 Three/WebGPU、模型、自由空间与后处理链制造沉浸感 | 欲望观察舱只转译固定相机、直接操控、场景层次与资源预算，使用原创原生 WebGL 程序化几何 + 语义 DOM | 保持低依赖、移动端预算、键盘和读屏回退，也避免复制未授权资产 | 程序化商品无法满足经验证的识别度，且已有独立模型来源与性能预算时 |

## 5. 资产来源登记

| 资产 | 来源 | 许可 | 仓库/部署决定 | 状态 |
| --- | --- | --- | --- | --- |
| 高清房间与小浣熊视觉包 | Figma 节点 `32:3631` 为当前构图基准；历史素材来自用户提供的 `视觉包2` | 用户确认为本项目可用素材 | 原文件保留；重新派生时必须保持红色凳子上坐着的小浣熊与足够全景清晰度 | 已使用 8192×4096 WebP，本地桌面/移动浏览器通过，待用户验收 |
| 开场与循环等待视频 | 用户授权 Figma 项目；循环等待对应节点 `4:5` | 用户已将当前账户加入项目并确认可导出 | 保留最高可用分辨率原导出与静态封面；网页只使用优化派生文件，导出失败必须登记 | 两段运行 MP4 均为 1920×1080；节点 4:5 无时间轴/内嵌视频，无法原生导出 4K 动态文件；4× 静态整帧 7680×4320 已导出 |
| 20 张人格视觉卡 | 用户授权 Figma 项目 | 用户确认为本项目可用素材 | 完整导出，不裁切主体；通过本地规则映射使用，不参与评分 | 20 张 4× PNG 已接入，本地映射、报告与保存流程通过 |
| 商城商品图片 | 用户授权 Figma 项目与版本化本地商品池 | 用户确认为本项目可用素材 | 原图/高倍导出登记来源，缺图时只使用本地可复现兜底，不让 AI 编造事实图 | 三类 Figma 原图/高倍整帧已导出，本地分类和订单同源联动通过 |
| AI 诊疗终端 | 项目原创 DOM / CSS 钱包病历本界面 | 项目自有 | 作为 P0 AI 人格与状态载体 | 基础原型已制作，专属动效待补 |
| 早期“钱包兽”占位 | 本项目 CSS 原创 | 项目自有 | 由用户提供的小浣熊主 IP 替换，避免出现两个角色体系 | 本轮移除 |
| 图标 | 优先自绘或明确商用许可 | 待登记 | 未登记前不发布 | 待选 |
| 参考项目模型、贴图、音视频 | samsy.ninja / 参考仓库 | README 明示归原作者 | 不复制、不入库、不部署 | 已排除 |

## 6. 主要风险

| 风险 | 可能性 | 影响 | 缓解方式 | 状态 |
| --- | --- | --- | --- | --- |
| 房间很酷但主功能不清楚 | 中 | 高 | 中央诊疗区主级、文字入口、10 秒识别测试 | 待 M1 |
| 过早做完整 3D 拖慢业务闭环 | 高 | 高 | P0 只做无依赖球形全景，完整 3D 必须单独证明价值 | 已控制 |
| AI 输出编造或与统计冲突 | 中 | 高 | 本地统计权威、结构校验、事实核对、降级 | 待 M3 |
| 本地数据被清理或跨设备丢失 | 中 | 中 | 明示风险、JSON/CSV 导出、版本迁移 | 待 M2/M4 |
| AI 密钥泄露 | 低 | 高 | 同源 Serverless 代理、构建产物扫描 | 待 M3 |
| 素材许可不清 | 中 | 高 | 全量登记，未知许可不用 | 持续 |

## 7. 里程碑日志

### M0 — 设计与技能基线（2026-08-25，已完成）

- **产出**：v1.1 PRD、`DESIGN.md`、本计划、项目内 `interactive-site-rebuild` Skill 和原创模板。
- **验收证据**：官方 `quick_validate.py` 返回 `Skill is valid!`；模板占位符只保留在模板标题；PRD、DESIGN 和建设计划对 P0 渲染、本地数据、AI 权限与素材边界的表述一致。
- **发现**：参考项目最值得借鉴的是状态、路由、热点和验证机制；自由移动不是本项目 P0 的必要条件。
- **下一入口**：校验 Skill，随后从 M1 的“房间壳 + 四个热点 + 诊疗终端 + URL/返回”竖切开始。

### M1–M2 — 全景房间与本地业务竖切（2026-08-25，本地已完成）

- **产出**：`panorama.js` 提供无依赖 WebGL 球形全景、拖动/缩放/键盘相机与热点投影；`scene-config.js` 统一登记三个核心功能物、五个快递箱热点和镜头焦点；`index.html`、`styles.css`、`app.js` 承载早期 CSS 钱包兽占位、内容面板和本地业务闭环。钱包兽存在是当前实现事实，不再是 P0 验收要求。
- **浏览器证据**：在 375×812、768×900、1440×900 三档视口检查；健身屏、茶几手机、白板和快递区均能定位；快递内心 OS 可逐个悬停/轻点；`Esc` 和浏览器历史可返回房间；直接刷新 `#goals` 可恢复目标面板；返回后焦点恢复；减少动态开关生效；当前干净刷新控制台为 0 错误、0 警告。
- **数据证据**：真实走通“新建 ¥89 冷静小票 → 自动进入订单收件箱 → 标记幸好没买 → 今日确认省下更新为 ¥89 → 新建 ¥1000 预算计划 → 显示 9% → 刷新 `#goals` 后仍恢复”。
- **修正**：实机首次检查发现健身屏和白板的全景经度方向写反、手机热点偏高、快递导航同时展开五条文案以及关闭面板时的 `aria-hidden` 焦点警告；已重新标定坐标、改为逐箱展示并使用 `inert` + 先恢复焦点消除警告。
- **边界**：钱包病历本当前是确定性的本地交互原型，并未接入真实 AI；AI 代理、同意摘要、Schema 校验以及超时/429/无效结构故障态仍属于 M3。
- **视觉边界**：当前是热点轨道与光晕，不是贴合物体外轮廓的描边；获得分层源文件或透明遮罩后再为手机、屏幕和白板补精细轮廓。
- **下一入口**：先做首轮场景理解测试并微调热点，再接真实 AI 的汇总数据代理和失败状态；CSV、重置和部署证据仍待补。

### M1.5 — 小浣熊主 IP 与像素瀑布入口（2026-08-26，本地已完成，待用户验收）

> 历史记录：下述内容是 2026-08-26 当时的实现证据。2026-09-01 已确认的新目标以“双阶段视频开屏、循环等待页唯一开始按钮、房间顶部三个语义入口”为准；旧像素瀑布和移动底栏不再是当前验收口径。

- **产出**：接入 8192×4096 高清全景和小浣熊引导派生图；新增原创 Canvas 像素瀑布、3 秒强制门禁、入口定格、相机拉远与房间交互解锁状态；早期 CSS 钱包兽由同一套小浣熊视觉替换。
- **时机证据**：以 `INTRO_LOCKED` 状态开始计时，2.4 秒时按钮仍不可见且禁用；再经过 0.9 秒后进入 `ENTRY_HOLD`，按钮才可见并启用。开屏期间拖动、滚轮、键盘相机和热点均被锁定。
- **浏览器证据**：1440×1000、768×900、390×844、375×812 均无横向溢出；桌面入口为左侧小浣熊、右侧标题与按钮，手机入口收敛为上方标题、下方角色；进入后桌面热点和移动底栏恢复。
- **韧性证据**：系统减少动态偏好下保留相同 3 秒门禁并改为低位移显现；高清全景请求失败时仍能结束开屏、显示入口并进入文字导航；干净会话控制台 0 错误、0 页面异常，样式表 `sheet` 正常挂载。
- **交互回归**：消费测试可从房间文字导航打开，URL 切换为 `#clinic`；`Esc` 返回 `#room`，面板隐藏并把焦点恢复到原“消费测试”入口。
- **下一入口**：由用户先验收入口镜头、标题版式、像素密度和主房间小浣熊占比，再决定是否调整节奏或进入手机浮起 / 白板便签等 M5 专属交互。

### M5-WHITEBOARD — 主全景白板可拖动便签（2026-08-26，本地已完成，待用户验收）

> 历史记录：下述多便签能力是 2026-08-26 的实现事实。2026-09-01 已确认活动版只保留一个当前确认省下目标和连续四态滑块；需要迁移旧数据并按新口径重新验收，不能把本节旧证据当作当前设计已完成。

- **产出**：右侧预算面板提供多便签列表、显著的“新建便签”、当前目标切换、编辑、颜色和删除；保存后，原创 DOM 便签生成到主全景画面里的实体白板，可在白板范围内拖动、用方向键移动，按 Enter 返回编辑。
- **空间绑定**：`scene-config.js` 集中保存原图白板四角的球面坐标；`panorama.js` 提供屏幕 / 球面双向投影。便签移动、镜头环视和缩放共用同一坐标系，不是固定在屏幕上的浮层。
- **数据边界**：便签位置、颜色和轻微旋转随各自目标保存在 LocalStorage；它们只影响展示，不改变确认省下、目标进度或 AI 事实版本。同时只有一个当前目标；订单首次进入“幸好没买”时才绑定当时目标，防止同一金额重复填满多张便签。
- **素材边界**：首版直接复用高清房间中的白板，不再在右侧伪造第二块白板；胶带和便签由 CSS 绘制。后续可增加白板前景边框 / 遮挡层，不改变交互数据。
- **浏览器证据**：1440×1000 桌面与 390×844 手机均完成截图检查；375、390、768、1440 四档无横向溢出。鼠标拖动后坐标从 `x=0.5649/y=0.49` 更新到 `x=0.6488/y=0.4147`；键盘大步移动、换色和 LocalStorage 持久化均通过。
- **交互证据**：移动端提交后 URL 从 `#goals` 回到 `#room`，镜头聚焦白板并显示便签；相机右移后便签屏幕横坐标从约 `35px` 变为 `173px`，本地便签坐标不变，证明便签随全景投影。干净会话控制台 0 错误、0 警告。
- **边界**：本轮使用浏览器指针事件覆盖鼠标和触控路径，尚未在真实手机硬件上验收；白板前景遮挡层仍属于素材增强项。

### M5.2 — 欲望观察舱 3D 交互（2026-09-04，本地实现，待用户验收）

- **产出**：保留书桌真实掌机和兼容路由 `#room?activity=peel`，将旧的抛接切割局替换为独立暗场观察舱。六类程序化商品在固定台座中可拖动旋转、键盘切换；每件商品有三条短信号，关掉两条后出现不消失的冷静问题。
- **业务边界**：活动无倒计时、分数、输赢或持久化状态；可读取最近一笔个人冷静单快照，但不写订单。点击“放进冷静单”只把商品带入新增表单，仍需用户确认保存；手势只旋转商品。
- **性能与无障碍**：当前商品与前后邻居构成最多三件的渲染预算，DPR 上限 `1.75`，连续慢帧按 `full → balanced → essential` 降级。商品档案、信号、问题和操作均为语义 DOM；减少动态进入 `essential`，WebGL 上下文丢失时切换静态商品视图。
- **浏览器证据**：1440×1000 的拖动、键盘切换、关信号、表单预填和 `Esc` 焦点恢复通过；375×812、390×844、768×900 无横向溢出。强制 WebGL 上下文丢失后静态视图与 10 个可见操作仍保留；控制台 0 错误、0 警告。
- **自动化证据**：观察舱核心与五组集成 / 布局测试 `30/30`，服务白名单 / 安全头测试 `24/24`，完整 `npm test` 为 `394/394`；全部必需语法检查、`git diff --check` 和 Compose 配置检查通过。
- **来源边界**：参考仓库只用于理解状态分层、固定相机、直接操控和资源预算；未复制其代码、模型、材质、贴图、音视频、品牌或文案，也未引入 Three/WebGPU。
- **未验证**：真实手机 GPU、真实手机触控与真实摄像头手势尚未验收；本地证据不等于生产环境。

## 2026-09-05 欲望追踪任务（本地实现与浏览器验证完成）

- 用户确认的方向：连续街区追踪、抓甩外壳、试用拖放、冷静传送带；耳机单商品纵向切片。
- 设计与来源登记：`docs/plans/2026-09-05-desire-tracking-mission-design.md`。
- 回退：独立工作树原提交 `9e7d578`。旧观察舱代码保留，默认入口改为新任务；未提交、推送或部署。
- 本地证据：`npm test` 397/397；桌面 1440×1000、平板 768×900、手机 390×844 与 375×667，浏览器 77 个检查点通过。详细证据与边界见 `docs/plans/2026-09-05-desire-tracking-mission-validation.md`。
- 业务边界：确认保存才创建 cooling 单；试玩的营销情节和选择不作为评分证据；存储失败保持临时记录提示。真实掌机与使用说明文字入口都可进入任务。
- 未验收：真实摄像头识别、真实手机 GPU / 触摸、系统后台状态和生产环境。当前等待用户本地体验反馈。

### 2026-09-05 追加鼠标与 WASD 控制（本地已完成）

- 用户追加方向控制后，夜城追踪支持 WASD / 方向键持续移动、松键停止和鼠标拖动视角；移动范围限制在街道内，靠近路标依序推进。保留点击或 Tab / Enter 自动前往，支持途中按键接管。
- 已补失焦与隐藏停止、过期按键清除、输入框隔离、短屏路标避让，以及触摸拖动后切换键盘的处理。
- 本轮 `npm test` 402/402、10 个 JS 语法检查与 diff 检查通过；浏览器键鼠、原任务流程、响应式和回退共 96 个检查点通过。完整结果见任务验证文档的追加章节。
- 保持本地状态，等待用户体验；没有提交、推送或发布。浏览器仿真不代表真实手机或系统后台验收。
