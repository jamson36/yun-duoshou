# 2026-09-05 全站交互审计

状态：本地修改、自动化和浏览器验证完成，已获用户授权进行本地提交。未推送、未上传、未部署。

本轮目标是检查主要点击、返回、表单、键盘、订单与目标联动及失败回退，修复可复现问题。保留全景入口和现有原生模块结构；评分公式、在线数据合同与发布方式没有变更。

## 本地环境

- 当前工作区：`/Users/ycsxy/Downloads/yun-duoshou`。
- 当前预览：<http://127.0.0.1:8788>，以 `PORT=8788 HOST=127.0.0.1 npm start` 启动。
- 8787 已有服务属于 `.worktrees/immersive-product-gallery`，已保留。该服务的早期探查不计入本轮最终验收，全部最终验证在 8788 完成。
- 浏览器中的订单均为本轮测试记录。需要清空数据的用例在新建、独立、无持久化用户配置的浏览器上下文中创建临时记录后执行；现有浏览器数据未重置。
- 本地在线服务未配置密钥；真实检查了无密钥降级，成功、限流、超时和无效结构使用浏览器响应注入验证。

## 修复与证据

| 问题 | 修复 | 验证结果 |
| --- | --- | --- |
| 快捷额度替换手动目标，丢失名称、创建时间及订单关联 | 快捷额度更新当前目标，保留其他内容、演示标记和关联；无目标时才创建 | 独立环境创建目标并确认省下 ¥120，调额 ¥1,000 → ¥3,000 后，ID、名称、创建时间和 ¥120 进度全部保留 |
| 订单弹窗 Tab 落入折叠的判断选项，焦点卡在 summary | 可聚焦列表排除关闭的 details 内容及隐藏、inert、禁用控件 | 折叠时可从选填入口继续到保存和关闭；展开后仍可操作选项 |
| 商品名或目标名只输入空格时，点击保存没有清晰反馈 | 使用原生表单约束拒绝纯空白内容 | 两个字段均阻止提交并聚焦错误字段 |
| 日期选择器在键盘切换页签后残留，关闭后仍可能延迟抢焦点 | 失焦时收起，延迟聚焦前检查打开状态，并选择可用日期 | 键盘切页、Esc、日期选择及关闭前动画帧均通过 |
| 浏览器后退留下表单、小票、分享或确认遮罩 | 历史同步前统一清理临时弹层，待确认操作按取消处理 | 商城预填后返回目录不保存；小票后退正常关闭；删除确认后退保留原订单 |
| 全景边缘存在屏幕外仍可聚焦的快递或活动热点 | 根据投影坐标隐藏视口外的非核心热点 | 四档布局矩阵未再发现屏幕外可操作控件 |
| 减少动态模式的辅助文案仍说“下方入口” | 更新为房间顶部三个功能入口 | 与实际导航位置一致 |

## 浏览器覆盖

| 范围 | 已执行 |
| --- | --- |
| 开屏与房间 | 普通访问单开始入口、完成测试后仅一次刷新直达房间、再次刷新恢复开屏；拖动、方向键、缩放；三个核心物件实际点击及返回后恢复原热点焦点；三个顶部入口 |
| 商城 | 购物、外卖、兴趣入口；三个目录所有可见分类；搜索命中及空结果；商品详情；预填；快速双击模拟只创建一笔；小票建议；订单入口；Esc、页面返回、浏览器后退 |
| 订单 | 手动创建、编辑金额、最多两项决策动作；冷静中 → 回血中 → 已剁手；统计与目标重算；删除确认安全焦点、Esc 与后退取消；JSON 导出 |
| 目标 | 空白名称、金额上限、快捷额度、珠子选择、日期跨月；单目标保存；调额保留目标身份和已有进度；控制器连续输入及键盘调节不写入业务数据 |
| 消费测试与报告 | 不足三笔锁定；三笔本地报告；未同意时不发请求；报告历史重开；按已剁手实际商品生成报告；分享预览和 Esc 焦点恢复 |
| 在线状态 | 真实 503 未配置降级；模拟成功、429、无效 JSON 结构、12 秒超时；各次结束后按钮恢复；撤回同意；聚合请求未包含 QA 商品名、订单 ID 或目标名称 |
| 帮助和设置 | 帮助关闭、焦点恢复、减少动态；重置取消；独立临时浏览器环境中的确认重置；空白环境导入七笔演示数据并检查标记及报告 |
| 欲望剥壳机 | 进入、教学、键盘与辅助按钮、完整 45 秒一局、18 次信号识别、结果选项、已有订单去重跳转、Esc 退出 |
| 故障回退 | 在独立环境注入 WebGL、全景及视频资源失败，目标仍能操作；LocalStorage 写入失败时可临时保存并明确提示刷新可能丢失 |

屏幕矩阵共 40 项：`1440×1000`、`768×1000`、`390×844`、`375×667`，各检查房间、购物介绍、手机首页、三个商城目录、订单、消费测试、目标、控制器。最终未发现横向溢出、无滚动容器可达的屏幕外控件或可见图片加载失败。长报告和订单使用页面原有滚动容器，不以内容超出首屏判为错误。

正常路径最终控制台为 0 条错误、0 条警告。故障注入中的 503、429、资源中止是预期结果，未当成正常路径通过证据。

本地健康接口只返回通用状态及配置布尔值；`/server.mjs`、`/.env`、`/server/diagnosis-service.mjs` 和本审计文档的 HTTP 路径均返回 404。控制器四类状态均已实际触发；导入演示数据的替换确认可通过 Esc 取消。

## 自动化和改动范围

- 修改前基线：391/391。
- 修改后：399/399，新增 8 个回归用例。更新旧入口缓存版本断言，游戏控制器缓存版本保持原有要求。
- `node --check`：`app.js`、`goal-date-picker.js`、`personality-scoring.js`、`panorama.js`、`scene-config.js`、`server.mjs`、`server/diagnosis-service.mjs` 通过。
- `git diff --check` 通过。
- 沙箱首次完整测试无法绑定本地端口；在允许本地监听的执行环境重跑通过，不将监听权限错误计为产品故障。
- 修改文件：`app.js`、`goal-date-picker.js`、`index.html`、`panorama.js`、`DESIGN.md`、`tests/interaction-audit.test.mjs`、`tests/peel-gallery-style.test.mjs`，以及本审计记录。
- 入口和被修改的浏览器模块已更新缓存参数；没有新增运行时公开模块，不需要扩大静态白名单或 Docker 拷贝清单。
- 现有未跟踪 Figma、图片和视频资产保留，没有覆盖或加入本轮改动。

## 证据与边界

脚本、布局结果、截图、导出样本及控制台日志位于本地忽略目录 `output/playwright/interaction-audit-20260905/`。代表截图：`receipt-mobile-stable.png`、`goal-preserved-mobile.png`、`peel-summary-mobile.png`、`controller-375.png`、`controller-1440.png`。完整测试日志保存为该目录下的 `npm-test.log`。

`results.json` 中 `goalsBeforeShortcutFix` 是发现快捷目标缺陷时的证据；修复后的结果见 `isolatedGoalResult`。本轮测试页面及独立浏览器上下文已关闭，当前 8788 本地服务保留供用户查看。

此次是桌面 Chromium 与移动视口自动化，未宣称真实手机、微信内置浏览器、真实摄像头或体感传感器验收。在线成功是受控响应验证，不代表真实上游可用。未执行生产检查或发布，也不能据此承诺所有输入和设备组合绝无缺陷；本轮覆盖路径没有剩余已知阻断问题。

现有浏览器的确认重置曾被自动审批拦截，原因是可能清空已有数据。该操作未执行；随后使用新建独立上下文、仅含当场创建的临时目标和订单，完成取消及确认重置验证并关闭上下文。
