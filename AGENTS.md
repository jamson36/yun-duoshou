# AGENTS.md

本文件适用于项目根目录及全部子目录。它用于把产品要求、消费人格规则、工程边界和协作流程转成后续 Agent 必须遵守的执行规范。

## 1. 项目身份

- 产品展示名固定为“让你花个爽！”。页面、演示、截图和对外文案不得再混用历史候选名称。
- 技术短名为 `spree`，仅用于包名、容器、服务器目录和域名等工程标识。
- 产品是“消费冲动缓冲器”，不是电商、支付、记账、存款或理财产品。
- 核心体验是：先记录想买的东西，再由用户决定“幸好没买”或“最终购买”，随后查看可解释的消费倾向和目标进度。
- 不接银行卡、不发起真实支付、不验证用户是否真的存钱，也不允许任何 AI 能力替用户执行订单、目标或资金操作。

当前发布目标的信息如下，但它们不构成自动发布授权：

- 公开地址：`https://spree.jamson.top`
- 服务器目录：`/home/spree`
- Compose 项目与容器：`spree`
- 宿主机应用监听：`127.0.0.1:8787`

## 2. 决策依据与冲突处理

执行时按以下优先级判断：

1. 用户在当前会话中的最新明确要求。
2. 本 `AGENTS.md`。
3. `让你花个爽！活动网页产品需求文档 (PRD).md`：产品范围、业务规则和验收口径。
4. `DESIGN.md`：空间交互、状态机、模块边界和技术设计。
5. `docs/plans/2026-08-25-consumption-personality-scoring-design.md`：评分公式、证据合同、人格门槛和置信度。
6. `REBUILD_PLAN.md`：里程碑、资产来源、偏差和待验证事项。
7. `README.md` 与其他计划文档：运行方法和背景信息。
8. 当前源码、测试和浏览器运行结果：只用于证明实际实现状态，不能反过来静默改写产品要求。

如文档之间或文档与代码发生实质冲突：

- 不得自行选择一个口径后继续大改。
- 先列出“冲突内容、当前实现、影响范围和建议方案”。
- 会改变产品范围、数据含义、评分结果、隐私边界或发布方式时，必须停下来询问用户。
- PRD 是活动版验收基线；评分设计等配套文档可以细化实现，但不得静默覆盖 PRD。

始终区分以下状态，不得互相替代：

- 文档已写
- 代码已实现
- 自动化测试通过
- 本地服务已启动
- 本地浏览器已验证
- 已提交 / 已推送
- 已上传服务器
- 已部署
- 生产环境已验证
- 现场或真实用户已验收

## 3. 强制开发与发布流程

默认流程固定为：**本地修改 → 本地启动 → 本地验证 → 展示结果 → 等待用户确认 → 再上传部署**。

### 3.1 本地阶段

1. 开始前查看相关 PRD、设计文档、源码、测试和工作区状态，不覆盖用户已有修改。
2. 只在本地项目中完成代码和文档变更。
3. 运行与改动风险相匹配的自动化测试、语法检查和浏览器验证。
4. 启动本地服务，让用户可以在本地页面查看结果。
5. 汇报本次变更、验证证据、已知限制和本地访问地址。
6. 明确停在“等待用户确认”状态。

### 3.2 发布门禁

只有用户在看到本地结果后，对本轮版本明确表示“可以部署”“上传吧”或同等含义，才允许执行本次发布。

- “继续做”“可以”“按这个方案”若发生在本地实现之前，不自动等于发布授权。
- 一次发布授权只覆盖当时已展示并确认的那一版；确认后又有实质修改，必须重新本地验证并重新确认。
- 未获得授权前，不得上传文件、远程写文件、重建或重启生产容器、修改 Caddy、修改 Cloudflare、切换 DNS 或执行其他生产变更。
- 只读生产检查也必须与当前任务相关；不得为了“顺便看看”扩大到其他站点或服务。
- 用户要求“先诊断”时只做只读诊断，不得顺手修复或发布。

### 3.3 发布后

获得授权并发布后，必须分别验证：

1. 应用健康接口。
2. 公开首页与本轮关键流程。
3. AI 成功或降级路径中与本轮相关的部分。
4. Caddy / Cloudflare 路由（仅在本轮改动涉及它们时）。
5. 服务器上原有站点未受影响。

汇报时明确写出上传、容器更新、路由更新和生产验证各自的结果；不能只用“已上线”概括。

## 4. 活动版产品边界

### 4.1 必须保留

- 无需登录即可体验。
- 移动端优先，同时兼容桌面端。
- 可环视的全景房间是首页和功能入口，不得退化成普通卡片导航页。
- 健身区屏幕对应“消费测试 / 钱包病历本”。
- 沙发茶几手机同时承载“假装下单 / 订单处理”。
- 白板对应“预算计划”。
- 快递堆只承担探索型消费内心 OS，不新增一级页面。
- 空间物件不是唯一入口；移动底栏和可键盘访问的文字导航必须始终可用。
- 订单、目标、设置、同意状态和最近一次诊断保存在当前浏览器；服务器不建立业务数据库。
- AI 不可用或用户不同意发送数据时，本地下单、订单、统计、人格、目标和导出仍应可用。

### 4.2 不得擅自增加

- 登录、账号、手机号、云同步或跨用户画像。
- 真实支付、银行连接、电商下单、资金划转或收益承诺。
- 投资、信贷、医疗、心理诊断或“购物成瘾”等结论。
- AI 自动修改订单、删除数据、修改目标或执行任何真实操作。
- 无限开放对话、跨会话 AI 记忆、多人、排行榜或社交。
- 为视觉效果引入自由行走、重型 3D、全屏视频或滚动劫持。已确认的 3 秒强制开屏是产品入口的一部分，不得自行延长、跳过或改成独立视频页。

这些属于需求扩展；若确实有价值，先说明收益、成本、隐私影响和验收方式，再等待用户决定。

## 5. 业务事实与数据不变量

订单状态必须遵守：

```text
冷静中 → 幸好没买
冷静中 → 最终购买
幸好没买 ↔ 最终购买（允许用户纠正）
任意状态 → 删除（需要二次确认）
```

- 新订单初始状态只能是“冷静中”。
- “冷静中”不计入确认省下，也不计入目标进度。
- 只有用户明确标记“幸好没买”的金额才计为“确认省下”。
- “最终购买”不得进入确认省下或目标进度。
- 目标进度只累计目标创建时间之后的确认省下金额。
- 删除、纠正状态或修改有效数据后，统计、人格、目标和场景反馈必须从源记录重新计算，不做不可追溯的增量猜测。
- 页面必须说明确认省下和回血进度是产品内的虚拟记录，不代表银行存款到账。
- 今日、7 天和 30 天按用户设备本地日期计算；金额统一按 CNY 和最多两位小数处理。
- 演示数据必须有明确 `demo` 标记，与个人数据在显示、分析和导出中可区分。
- LocalStorage 不可用时允许临时体验，但必须提示刷新后可能丢失，不能假装已经保存。

## 6. 消费人格与评分硬规则

评分是可解释的近期消费倾向，不是心理测验，也不是永久人格。

### 6.1 输出结构

本地规则引擎应分别计算：

1. 五个行为维度：即时冲动 `I`、价格敏感 `P`、研究验证 `R`、外部依赖 `X`、情绪体验 `E`。
2. 四类购买动机：掌控、疗愈、成就、认同；占比合计为 100%。
3. 行为结果：冷静单数率、冷静金额率、购买转化率、中位决定时间、反复率和待冷静率等。
4. 人格表达：一个主人格，满足条件时一个副人格；最高频品类只生成“钱包主题”。

人格库为：研究主权者、优惠精算师、种草冲锋手、氛围疗愈师、社交共鸣者、微观成就家、极简缓冲者、收藏囤货派、平衡洞察者。无人格达到门槛时使用“欲望观察员”。

### 6.2 计算与证据

- 同一输入和同一规则版本必须得到同一结果。
- 分数、人格、金额、次数、排名、置信度和目标进度的唯一事实来源是本地版本化规则。
- 优先使用真实时间和状态历史、用户主动选择的决策动作、结构化触发原因。
- 自然语言提取只可作为降权证据；置信度低于规则门槛时不得进入分数。
- 品类只能作为主题或极弱动机证据，不得用于推断人格、年龄、性别、收入、健康、地域、文化身份或社会阶层。
- 单次大额消费不能直接被判为冲动；金额只能在有足够样本时做有上限的权重修正。
- 某维度没有直接或结构化证据时返回 `null`，页面显示“证据不足”；不得用 50 分伪装成真实中立。
- 少于 3 笔有效订单时不生成人格；3–4 笔只能称“初步倾向”。只有有效样本量和置信度达到规则门槛后，才可称“相对稳定的近期画像”。
- 主人格、副人格、收藏囤货派和平衡洞察者必须执行评分设计中的最少证据与硬门槛，不得为了丰富结果绕过。
- 每个主要判断必须能回溯到证据；允许用户纠正行为字段，纠正后重新评分。

具体权重、半衰期、公式、门槛和置信度以评分设计文档为准，不在多个文件中复制第二套实现。

### 6.3 表达边界

- 使用“从最近 30 天的记录看”“近期倾向”“当前证据不足”等表述。
- 不使用“科学证明你是”“你就是冲动人格”“消费观有问题”“成瘾”“病态”“失控”等诊断或羞辱文案。
- 同时给出证据、判断、限制和一个低压力、可执行、可撤销的小建议。
- 购买转化率不称为失败率；冷静成功也不包装成道德优越。

## 7. 在线解释能力的职责与隐私

### 7.1 权限边界

在线能力只能：

- 根据本地评分结果解释模式。
- 从服务端给定的证据目录选择依据。
- 生成一步建议和目标关联。
- 在明确契约下提取候选文本证据。

在线能力不得：

- 自己决定或覆盖分数、人格、置信度、金额和次数。
- 编造未记录的商品、订单、行为、目标或数值。
- 绕过硬门槛输出更“有趣”的人格。
- 修改订单、目标、同意状态、本地数据或导出文件。
- 推断敏感身份或给出专业财务、医疗、心理判断。

### 7.2 明示同意与最小数据

- 首次发起在线分析前必须展示用途、发送范围和不包含内容，并获得用户主动确认。
- 只发送选定周期的聚合统计、本地评分、人格结果、置信度、证据目录和必要的目标进度。
- 默认不发送商品名称、备注、订单 ID、单笔消费时间、导出文件、浏览历史、真实身份、支付或银行信息。
- 拒绝或撤回同意不影响本地核心功能；撤回后下次分析必须重新确认。
- 订单或目标数据变化后，旧诊断标记为过期，提示重新分析；不得继续当作当前结论。
- 网络超时、限流、上游错误、无效 JSON 或 Schema 不合格时，明确显示降级，并继续提供本地结果。

### 7.3 不暴露服务商和模型

- 用户可见页面、按钮、状态、错误提示、分享图、截图、演示话术和公开 API 返回中，不得出现在线服务商、模型名称、模型版本或可识别供应商的措辞。
- 用户侧文案优先使用无主语表达，例如“开始复诊”“复诊中”“复诊完成”“暂时无法复诊”，不要出现“某某为你分析”。
- `/api/health` 只可返回通用服务状态和是否已配置，不返回模型或服务商。
- `/api/ai/diagnosis` 不向浏览器返回 `provider`、`model` 等字段。
- 公开静态资源不得包含可搜索到的供应商或模型名称。
- 内部适配代码和服务端环境变量可以保留现有技术字段；不要为了隐藏用户界面而随意改动内部适配协议。

### 7.4 密钥与日志

- 密钥只存在于服务端环境变量或服务器 `.env`，绝不写入前端、镜像、测试夹具、截图、文档示例值、命令输出或聊天回复。
- 不读取、打印、复制或回传服务器上的真实密钥；验证只检查“是否配置”。
- `.env` 不提交、不上传到公开位置；部署时保留服务器已有 `.env`。
- 服务端日志只记录请求 ID、耗时、状态和错误类型，不记录完整输入、完整输出或可还原单笔消费的数据。
- 所有用户输入按纯文本渲染，不直接作为 HTML。

## 8. 空间交互与视觉规则

- `图片 3.png` 是用户确认的最终场景构图基准；浏览器优先加载其派生资源 `assets/room-panorama.webp`。
- 热点坐标和物件映射集中维护在 `scene-config.js`，房间热点、文字导航和自动化测试不得各自维护一套不一致的定义。
- 点击核心物件时，应在约 300–500ms 内完成镜头转向 / 推近和物件聚焦，同时立即开放内容，不能等待动画结束才允许操作。
- 聚焦反馈优先使用物件放大、发光、抬起、转正、扫描、盖章或归档等与功能直接对应的动作。
- 同一时间只激活一个区域；关闭、`Esc` 或浏览器后退后物件归位，焦点返回原触发入口。
- 业务表单、订单详情、评分和诊断结果必须使用语义化 DOM，不绘制进 WebGL。
- 移动端底部导航是权威替代入口；不允许通过横向滚动强行展示全部房间功能。
- 支持键盘操作、可见焦点、至少 44×44px 点击区域、非颜色唯一状态、合理的辅助技术播报。
- 启用“减少动态效果”时移除视差、旋转、循环动画和大幅位移，用淡入、状态章和短距离变化替代。
- WebGL、全景图或装饰资产失败时，今日数据、文字导航和业务功能必须继续可用，不能白屏。
- 小浣熊是产品主 IP，开屏与房间内必须保持同一角色形象；它负责世界观、引导与情绪反馈，但不是 AI 能力的唯一入口。
- AI 仍由钱包病历本 / 诊疗终端承载。小浣熊、开屏或场景资产失败时，文字导航与业务功能仍须可用。
- 参考网站只借鉴机制和设计语言，不复制品牌、文案、代码、模型、贴图、图片、视频或其他未授权素材。

## 9. 工程边界

当前项目保持低依赖结构：

- `index.html`：语义化页面骨架和面板结构。
- `styles.css`：响应式、状态、动效和视觉样式。
- `app.js`：Hash 路由、订单状态、本地存储、内容层和在线诊断前端状态。
- `personality-scoring.js`：纯函数式、可复算的评分与最小化汇总。
- `panorama.js`：轻量球形全景、相机和热点投影。
- `scene-config.js`：热点注册表和焦点参数。
- `server.mjs`：静态资源、同源 API、限流、尺寸限制、安全头和错误映射。
- `server/diagnosis-service.mjs`：上游适配、证据目录、解析、Schema 约束和事实回填。

实现要求：

- 前端继续使用原生 ES Modules、语义化 DOM 和响应式 CSS；除非用户确认架构升级，不引入框架或构建链。
- 活动版不增加后端数据库。
- 评分函数保持纯净；视图层不得另算一套分数或人格。
- 场景层只投影业务状态，不保存订单和评分事实。
- 路由是可分享页面状态的权威来源，场景状态不得制造另一套页面真相。
- 服务器只公开白名单静态资源和 API；服务端源码、环境文件和内部目录必须返回 404。
- 静态资源带 1 小时缓存；修改 JS/CSS 后需要检查入口的缓存版本，避免生产继续加载旧资源。
- 以最小、可审查的修改满足需求，不做无关重构，不覆盖或回退用户及其他协作者的改动。

## 10. 本地运行与验证

### 10.1 启动方式

需要验证完整同源服务和接口时：

```bash
npm start
```

默认访问：`http://127.0.0.1:8787`。

只验证纯前端和本地评分时可以使用：

```bash
python3 -m http.server 4173
```

默认访问：`http://127.0.0.1:4173`。

不得把真实生产密钥复制到本地或写入命令。需要在线成功路径时，使用用户明确提供的本地环境或受控测试配置；否则验证无密钥降级路径。

### 10.2 每次代码变更的基础门禁

至少执行：

```bash
npm test
node --check app.js
node --check personality-scoring.js
node --check panorama.js
node --check scene-config.js
node --check server.mjs
node --check server/diagnosis-service.mjs
git diff --check
```

只检查本轮相关文件；若某命令因环境或工作区状态无法执行，明确记录，不能写成已通过。

涉及容器配置时额外执行：

```bash
SPREE_ENV_FILE=./deploy/spree.env.example docker compose config --quiet
```

### 10.3 浏览器验收

前端或交互变更至少验证本轮相关路径，并覆盖：

- 桌面 `1440×1000` 与移动端 `390×844`；布局类变更再补 `375px`、`768px`、`1440px` PRD 三档。
- 无横向溢出、遮挡、不可点击控件或阻断性控制台错误。
- 房间拖动、三个核心热点、文字导航、移动底栏、返回、浏览器后退、`Esc` 和焦点恢复。
- 新建订单 → 冷静中 → 幸好没买 / 最终购买 → 数字、人格和目标重算。
- 不足 3 笔、初步人格、有充分证据、缺失维度和数据变化后结果过期。
- 首次同意前无请求、拒绝后本地能力可用、在线成功、超时 / 限流 / 无效结构降级。
- 刷新后的本地数据恢复、演示数据区分、重置确认和本地导出。
- 减少动态效果与全景资源失败回退。

只要求验证与本轮改动相关的最小充分集合；高风险或跨模块变更才执行完整矩阵。浏览器自动化通过不等于真实移动设备或现场验收。

### 10.4 隐私回归

涉及在线能力、服务端或用户文案时，必须验证：

- 公开静态页面和脚本中没有服务商或模型名称。
- 健康接口没有模型、服务商或密钥字段。
- 诊断响应没有 `provider`、`model` 或秘密信息。
- 请求负载没有商品名、备注、订单 ID 和真实身份字段。
- 日志没有完整输入输出或用户消费明细。
- 服务器源码、环境模板和内部目录不能通过公开地址读取。

## 11. 部署规范（仅在获得本轮明确授权后）

### 11.1 上传前

- 重新确认本地工作区就是用户刚刚验收的版本。
- 列出将上传的文件和不上传的本地文件；不得上传 `.env`、`.git`、测试浏览器状态、临时输出或真实密钥。
- 先检查服务器现有目录、容器和反向代理配置，备份会被修改的配置。
- 保留 `/home/spree/.env`，不要用本地空模板覆盖。
- 上传使用明确文件清单或安全排除规则；未经单独确认不要使用会删除远端文件的同步参数。

### 11.2 应用更新

标准应用更新目标为：

```bash
cd /home/spree
docker compose -p spree up -d --build
```

容器继续遵守：非 root、只读文件系统、移除 capabilities、限制 CPU / 内存 / 进程数、日志轮转、端口只绑定 `127.0.0.1:8787`。

只有域名或路由确实需要变化且用户也授权时，才修改 Caddy 或 Cloudflare。修改 Caddy 前必须备份、验证配置，再重载；失败时恢复备份。Cloudflare 变更需要记录原值和回滚路径。

### 11.3 生产验收与回滚

- 验证 `/api/health`、首页、核心静态资源和本轮关键业务流。
- 检查公开响应不暴露模型、服务商或密钥。
- 同时确认服务器上的其他已有站点仍能正常访问。
- 记录当前镜像 / 目录备份或上一版本的可回退入口。
- 验证失败时先停止扩大变更，优先回滚到上一可用版本，再报告证据。

## 12. 变更与交付说明

- 开始编码前说明本轮目标和不会触碰的边界。
- 工作过程中发现需求不清，若会影响产品行为、隐私、评分或发布结果，停下来问用户；纯实现细节可按现有架构做最小合理假设。
- 改变评分合同、数据结构、API Schema、路由或部署方式时，同步更新对应设计文档或新增配套说明；不要静默修改冻结的 PRD。
- 最终汇报先写结果，再列改动文件、测试、浏览器证据、未完成项和当前处于“本地”还是“生产”。
- 不得把计划、推测、测试夹具、容器健康或 HTTP 200 单独描述成完整产品验收。
- 提交前先确认当前目录是 Git 仓库并查看用户已有改动；只精确暂存本轮文件，不使用 `git add .`。
- 未经用户要求，不自动提交、推送或部署。
- 若用户要求提交，提交信息使用以下格式，并按“新增功能 → 逻辑优化 → Bug 修复 → 测试 → 用户体验 → 文档”的优先顺序组织：

```text
<type>(<module>): <summary>

- 在<模块>中新增<功能>
- 优化<接口或逻辑>
- 补充或完善<测试>
- 调整或优化<用户体验>
```

不存在的条目可省略，但剩余条目的逻辑顺序不变。
